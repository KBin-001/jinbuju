# 02 — 页面：悬浮计时条与日期、目标作用域解耦

**What to build:** 核心修复。今日页加载数据时改用 `getActiveActionSessionContext` 解析活跃会话及其任务，不再用「当前激活目标的作用域任务列表」去 `find` 会话关联的任务。移除「仅当 selectedDate === today 时才读取活跃会话」的门控——悬浮计时条在任意选中日期下都应反映全局活跃会话。悬浮条 `visible` 条件归结为「存在活跃会话」，不再要求 `activeSessionTask` 同时非空。今日任务列表的装饰逻辑（标记某行为「正在专注」）仍基于会话 `taskId` 与当前列表匹配；若会话任务不在当前列表里（顺延/跨目标），列表不标红但悬浮条仍显示——二者解耦。同时复核：解耦后非今日日期下今日摘要数字不误算入活跃会话的进行中投入。

**Blocked by:** 01 — 服务层：活跃会话上下文解析器

**Status:** done

- [x] 今日页 `load()` 通过 `getActiveActionSessionContext` 解析活跃会话任务，而非 `getTasksByGoal(...).find` 形式的目标作用域查找
- [x] 移除 `selectedDate === today` 对活跃会话读取的门控
- [x] 悬浮计时条 `visible` 条件最终归结为「存在活跃会话」（session 存在即显示，task 为 null 时也显示——恢复态由 Ticket 03 处理）
- [x] 日历切到非今日日期时，运行中的计时条不消失
- [x] 计时任务的 `status` 变为 `rescheduled` 后，计时条不消失
- [x] 切换激活目标后，原目标下正在进行的计时条不消失
- [x] 非今日日期下今日摘要不误叠加活跃会话的进行中投入（复核 `sessionMinutes` 仅在今日摘要场景叠加）
- [x] 今日任务列表装饰逻辑与悬浮条解耦：会话任务不在当前列表里时，列表不标红但悬浮条显示
- [x] 页面契约测试更新并通过（扩展已有契约测试文件，不新建测试文件）
- [x] 四个主 Tab 可正常进入，返回链路正常
