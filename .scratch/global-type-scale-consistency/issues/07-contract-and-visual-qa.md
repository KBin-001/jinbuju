# 07 — 收口硬编码字号并完成全局验收

**What to build:** 在全部页面迁移完成后收紧全局排版契约，清理无调用的过渡用法和无理由硬编码字号，并通过自动化检查与跨设备视觉对比证明四个主 Tab、子页面和状态界面已经形成一致、可维护的字号体系。

**Blocked by:** 02 — 修复进度页“教练观察”后的字号断层；03 — 迁移今日页与行动流程字号；04 — 迁移小队及成员流程字号；05 — 迁移“我的”及个人数据流程字号；06 — 迁移剩余页面与全局界面字号。

**Status:** claimed

- [x] 全局排版契约扫描全部产品页面、组件、样式和自定义底部导航，并排除第三方组件源码。
- [x] 常规文字不得残留无归属的裸 `rpx` 字号；所有保留例外均能被扫描器识别且有明确理由。
- [x] 响应式规则只引用语义令牌或批准的小屏层级，不再新增任意字号。
- [x] 四个主 Tab 的页面标题、分区标题、卡片标题、正文、说明、标签和核心数据通过契约验证同角色一致。
- [x] 旧的具体字号断言全部改为语义层级断言，不再保护已确认的视觉问题。
- [x] 无调用的兼容别名和临时迁移规则被安全清理；仍有调用的兼容项保留并记录原因。
- [ ] 使用同一组真实数据完成今日、进度、小队、我的横向截图对比，确认不存在明显一大一小。
- [x] 进度页连续滚动经过行动趋势、教练观察、成长记录和最近完成时没有字号断层。
- [ ] 视觉验收覆盖常见 iPhone、窄屏 Android、宽屏、系统字体放大、长文本、加载、空数据和错误状态。
- [ ] 全部现有 Node 测试、排版契约、页面视觉契约、TypeScript 类型检查、微信开发者工具编译和 npm 构建通过。
- [x] 最终改动不包含第三方源码修改、新 UI 框架、调试日志、临时截图、无用资源或业务数据变更。

## Answer

已完成静态契约收口：新增 `test/type-scale-contract.test.js` 扫描产品 WXSS/组件/自定义导航并排除第三方源码，更新行动会话和进度页的旧字号断言为语义令牌断言。当前 55 个 Node 测试中 52 个通过；剩余 3 个是本次字号迁移之外的既有契约漂移（统一时长配置、AI 安全断言、今日教练状态模板），未改动其业务逻辑。当前环境没有微信开发者工具和 npm build 脚本，真实设备截图对比、IDE 编译和 npm 构建需在对应环境补验，因此本议题保持 claimed，等待 [map](../map.md) 中记录的外部验收。
