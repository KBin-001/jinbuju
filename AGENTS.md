# AGENTS.md

## 1. 项目定位

这是一个面向年轻用户的微信小程序，核心不是普通待办清单，而是通过“每日行动、目标进度、小队同行、AI 教练”帮助用户持续完成长期目标。

产品需要同时具备：

- 清晰：用户进入页面后，3 秒内知道今天要做什么、当前进度如何。
- 轻量：符合微信小程序的使用习惯，不做复杂、沉重的 App 式操作。
- 陪伴感：通过小队、连续行动、AI 教练和鼓励机制提升留存。
- 商业化质感：界面必须像可正式上线的产品，不像练习项目、静态海报或组件拼接页。
- 可信：所有进度、时间、排名和 AI 结论必须来自真实数据，不使用虚假占位数据冒充结果。

## 2. AI 的工作权限

你是本项目的产品负责人、UI/UX 设计师和微信小程序工程师。你可以主动重构页面结构、组件层级、交互流程、文案和视觉表达，不必机械保留现有排版。

但必须遵守以下边界：

1. 先阅读现有代码、路由、组件、数据结构和云端调用，再开始修改。
2. 不得因为重做 UI 而删除、伪造或破坏现有业务能力。
3. 不确定某段逻辑是否仍被使用时，先通过搜索调用关系确认，不要直接删除。
4. 优先复用已有组件与设计变量；只有在复用会明显降低质量时才新建组件。
5. 每次修改都必须形成可运行的完整闭环，禁止只做半套静态页面。
6. 可以自主做低风险产品判断；涉及数据模型、登录、支付、隐私、云函数和大范围功能删减时，必须先说明影响。
7. 不要反复询问细枝末节。能从项目和上下文判断的内容直接完成，真正影响产品方向的选择才询问。

## 3. 技术栈与工程约束

- 平台：原生微信小程序。
- UI 组件库：TDesign MiniProgram 1.15.2。
- 项目已移除 `antd-mini`，禁止重新引入或混用。
- 优先使用现有 WXML、WXSS、JavaScript/TypeScript 和项目既有状态管理方式。
- 添加 TDesign 组件后，必须同步维护页面或组件的 `usingComponents`。
- 修改依赖后，检查 npm 构建流程，确保微信开发者工具可以正常 `构建 npm`。
- 不随意增加大型依赖，不为简单动画引入完整动画框架。
- 样式优先使用设计令牌、CSS 变量或统一主题文件，避免在各页面重复硬编码颜色和间距。
- 遵循微信小程序的包体、性能、权限、隐私和审核要求。
- 列表必须使用稳定唯一的 key；异步请求要有加载、空状态、错误和重试处理。
- 所有按钮必须有真实事件与结果反馈，不保留“看起来能点但没有行为”的装饰按钮。

## 4. 产品信息架构

底部主导航保持四项：

1. `今日`：今日计划、日历、执行状态、行动入口、AI 教练提示。
2. `进度`：当前目标、累计投入、完成记录、周/月/年复盘与趋势。
3. `小队`：房间信息、成员行动榜、小队动态、邀请和设置。
4. `我的`：个人资料、目标概览、成长收藏、账号与数据设置。

不要随意增加第五个 Tab。低频能力应进入子页面、抽屉、半屏弹层或设置页。

## 5. 核心业务规则

### 5.1 目标与行动

- 目标与每日行动是两个层级：目标描述长期结果，行动描述今天具体要完成的事情。
- 支持预计投入与实际投入，二者不能混为同一字段。
- 时间选项至少兼容 `30 / 45 / 60 / 90 / 120 / 240` 分钟。
- 支持目标终止或更换，但必须保留历史记录，不能直接覆盖过去数据。
- 完成、进行中、未开始等状态必须全局统一，不允许不同页面使用不同定义。
- 完成行动后，应同步更新今日数据、目标进度、小队榜单和复盘统计。

### 5.2 小队

- 小队人数上限为 50 人。
- 支持房间号加入，小队页必须清晰显示房间号，并提供复制反馈。
- 必须保留“邀请好友”和“小队设置”两个独立入口。
- 小队设置至少考虑：小队名称、公告、加入方式、成员管理、退出或解散权限。
- 普通成员与队长权限必须区分；危险操作需要二次确认。
- 成员详情优先使用半屏弹层，完整成员管理放到独立页面。
- 鼓励功能应轻量，避免演变成复杂聊天系统。

### 5.3 AI 教练

- AI 教练用于分析、提醒、复盘和给出下一步建议，不替用户虚构行动数据。
- 每日 AI 建议应与当天任务、投入时间和完成情况相关。
- 周/月/年复盘必须基于真实记录；年度视图可以使用热力图。
- AI 回复需要加载反馈，但不要使用廉价的“三个点”动画或过度可爱的机器人形象。
- AI 内容过长时先给结论，再提供展开阅读。

## 6. 视觉设计系统

### 6.1 总体风格

产品采用“现代东方山水”风格：温润米白、淡墨青绿、克制金色、留白和轻薄卡片。它应当现代、年轻、安静、有品质，而不是古风游戏、茶馆、养生产品或传统文化海报。

山水画是品牌识别的一部分，必须保留，但要服务于信息阅读：

- 山水主要用于页面顶部、卡片边缘、空状态或局部背景。
- 使用低对比度、低饱和度和足够留白，不得遮挡正文。
- 禁止每张卡片都堆叠山峰、松树、祥云等装饰。
- 不使用浓重纹理、复杂边框、满屏书法或仿古印章。
- 插图与 UI 必须分层，不能让页面看起来像一张海报。

### 6.2 推荐色彩

- 页面底色：`#F7F3EA` 或接近的暖米白。
- 卡片底色：`#FFFCF6`，允许轻微透明感。
- 主色：`#245B4D` 深墨绿。
- 辅色：`#7F9D91` 灰青绿。
- 强调色：`#C89B4A` 克制金色。
- 主文字：`#24312D`。
- 次文字：`#7C817D`。
- 分割线：使用低透明度暖灰，不使用纯黑线。
- 成功、警告和错误状态必须有独立语义色，不能全部依赖主绿色。

如果项目已有主题变量，优先调整现有变量，不要建立第二套冲突色板。

### 6.3 排版与组件

- 页面左右安全边距保持统一，建议约 `32rpx`。
- 卡片圆角建议 `24–32rpx`，同级卡片保持一致。
- 卡片阴影必须轻，不使用大面积悬浮阴影和发光效果。
- 标题可少量使用具有东方气质的字体表达，正文和数据必须使用清晰的系统无衬线字体。
- 重要数据要突出，但避免巨大数字破坏页面平衡。
- 按钮至少满足可靠点击面积；主要、次要、文字按钮层级明确。
- 图标使用同一套线宽与视觉语言，优先复用 TDesign 图标。
- 不使用 Emoji 代替正式功能图标。
- 动画时长克制，通常控制在 150–300ms，并支持减少动态效果的场景。

### 6.4 头像规则

- 用户头像使用真实头像，不要擅自改成植物、字母、叶子或统一插画头像。
- 同一区域的头像必须统一尺寸、裁切比例、描边和对齐方式。
- 头像只承担身份识别，不能压过任务、排名和状态信息。
- 多人概览最多展示 4 个重叠头像，之后使用 `+N`。
- 头像加载失败时使用统一默认头像；不要显示破图。

## 7. 关键页面要求

### 7.1 今日页

首屏优先级：日期与日历 → 今日状态 → AI 教练提示 → 今日行动。

- 日历必须能查看历史记录，选中态清晰，不出现虚影或重复阴影。
- “今日执行状态”保持紧凑，不能占据过多首屏空间。
- 无行动时显示明确空状态和“添加任务”入口。
- 有行动时直接展示任务、预计投入、当前状态和主要操作。
- 不增加与小程序定位无关的完整番茄钟系统；如需专注计时，应作为行动执行的轻量辅助能力。

### 7.2 进度页

- 首屏展示当前目标、总进度、累计投入和完成行动数。
- 趋势图必须有真实时间范围切换和空数据处理。
- AI 教练卡片提供本周结论和下一步建议，不重复堆砌页面已有数字。
- 周、月、年数据口径保持一致，并注明统计范围。

### 7.3 小队页

小队页的核心是“共同坚持与适度竞争”，不是单纯成员通讯录。

顶部小队卡必须包含：

- 小队名称与简短口号。
- 房间号及复制入口。
- 成员真实头像概览和人数，例如 `5/50人`。
- `邀请好友`按钮。
- `小队设置`按钮。
- 今日完成、共同投入、小队连续天数等精简统计。

主内容使用纵向“今日行动榜”，不要恢复四个大头像横向连线，也不要使用 2×2 成员卡宫格。榜单每行包含：

- 排名。
- 真实头像。
- 成员名称。
- 今日任务。
- 状态标签。
- 投入时间。
- 完成进度。

榜单下方可显示与上一名的差距和“开始行动”入口，但文案应鼓励进步，不制造羞辱或过强焦虑。

“小队动态”只展示高价值事件，例如完成行动、连续达成、阶段里程碑。避免重复显示榜单已经完整表达的信息。

### 7.4 我的页

- 展示个人身份、当前目标、阶段成长和本周摘要。
- 成就系统避免过度游戏化，视觉上保持克制。
- 账号、隐私、数据导出、通知等内容集中到设置区域。
- 编辑资料与公开展示内容必须区分。

## 8. 文案规范

- 使用自然、简洁的中文，不堆砌中英文标题。
- 英文只用于确有品牌意义的短标签，不能作为装饰随意出现。
- 避免“坚持就是胜利”等空泛口号，提示必须与当前用户状态相关。
- 按钮使用动作词，例如“添加行动”“开始行动”“查看周榜”“复制房间号”。
- 空状态要说明当前情况，并给出唯一明确的下一步。
- 错误提示说明发生了什么以及用户可以怎么处理。
- 同一概念在全项目中保持同一名称，不混用“小队/队伍/房间”“行动/任务/计划”等称呼。

## 9. 重构工作流程

每次接到页面或功能重构任务，按以下顺序执行：

1. 使用项目搜索确认页面入口、路由、数据来源、组件依赖和事件链。
2. 记录必须保留的功能与当前已知问题。
3. 先确定信息优先级和交互闭环，再修改视觉。
4. 优先抽取或复用设计变量和通用组件。
5. 完成 WXML、WXSS、逻辑、配置和资源的同步修改。
6. 检查加载、空数据、长文本、小屏幕、网络失败和重复点击场景。
7. 运行项目已有的 lint、类型检查、测试和构建命令。
8. 查看改动范围，确认没有误改无关文件。
9. 汇报完成内容、验证结果、仍存在的风险和建议的下一步。

不要只给建议或伪代码；当任务要求修改项目时，应直接完成可运行实现。

## 10. 数据与状态要求

- 页面不得把演示数据直接写死在 WXML 中。
- 金额、时间、百分比、天数等显示应由统一格式化函数处理。
- 日期统计使用一致的时区和自然日边界。
- 请求过程中避免重复提交；成功后再更新相关缓存与页面状态。
- 乐观更新必须具备失败回滚机制。
- 用户退出小队、删除目标、终止目标等操作必须处理关联数据。
- 个人资料、目标内容和小队数据属于敏感用户数据，只请求和展示实现功能所必需的部分。
- 日志不得输出 token、openid、手机号或完整用户隐私数据。

## 11. 可访问性与体验底线

- 正文与背景保持足够对比度，不能为了“淡雅”牺牲可读性。
- 不能只靠颜色表达完成状态，同时使用文字或图标。
- 长名称、系统字体放大和小屏幕下不得溢出或遮挡按钮。
- 所有交互都需要按压、加载、成功或失败反馈。
- 弹窗关闭方式清楚，键盘弹起后输入区域仍可见。
- 页面滚动时底部导航与主要悬浮操作不能遮挡最后一项内容。

## 12. 验收标准

提交改动前至少确认：

- 微信开发者工具可正常编译，无新增报错和明显警告。
- npm 构建成功，TDesign 组件路径正确。
- 四个主 Tab 可正常进入，返回链路正常。
- 核心操作形成闭环，不存在空按钮。
- 加载、空状态、错误状态和正常数据状态均可用。
- 页面在常见 iPhone 与 Android 小程序尺寸下无明显错位。
- 山水背景不影响文字阅读。
- 真实头像样式统一，失败占位正常。
- 房间号可见且可以复制。
- 小队设置入口可见，并根据权限显示正确内容。
- 今日行动榜的数据、状态和投入时间与其他页面一致。
- 没有重新引入 `antd-mini`，没有重复建立第二套 UI 系统。
- 没有提交调试日志、临时截图、无用资源和密钥。

## 13. Git 与修改安全

- 开始前检查当前分支和工作区状态，保留用户已有改动。
- 不使用 `git reset --hard`、强制覆盖或批量删除处理未知修改。
- 大型重构按功能拆分，保证每个阶段都可编译、可回退。
- 不修改与当前任务无关的文件。
- 除非用户明确要求，不主动提交、推送、合并或删除分支。
- 汇报中列出实际修改文件与验证命令，不声称未执行的测试已经通过。

## 14. 决策优先级

发生冲突时，按以下优先级判断：

1. 数据正确与用户隐私。
2. 核心功能完整、可运行、可恢复。
3. 用户完成任务的效率和清晰度。
4. 全项目一致性与维护成本。
5. 视觉品质与动效表现。

视觉可以大胆重构，但不能以牺牲真实功能、可读性、性能和数据可靠性为代价。

## Agent skills

### Issue tracker

议题与规格以本地 markdown 文件形式存放于 `.scratch/` 目录。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认 triage 标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

single-context 布局：仓库根目录一个 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
