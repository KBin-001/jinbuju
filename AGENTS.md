# AGENTS.md

## 项目目标

这是微信原生小程序“进步局”。V1 聚焦单目标、AI 生成 7 天计划、每日打卡、轻量小队和连续行动反馈。

## 技术约束

- 使用 TypeScript、WXML、WXSS 和 CloudBase。
- 优先使用微信与 CloudBase 原生能力，不引入不必要的第三方依赖。
- 前端不得保存 AI 密钥，不得直接调用需要密钥的 AI 接口。
- 所有 AI 请求必须经过云函数。
- AI 输出必须使用固定 JSON Schema，并在云函数中做运行时校验。
- 所有数据库写操作必须从云函数上下文获取并验证 `OPENID`，不得信任前端传入的用户 ID。
- 时间字段统一存储为数据库服务端时间；业务日期明确使用 Asia/Shanghai。

## V1 范围

必须实现：

- 用户初始化
- 创建单个目标
- AI 生成结构化 7 天计划
- 今日任务与每日打卡
- 连续打卡天数
- 3～5 人小队成员进度
- 固定鼓励文案
- 社群二维码入口
- 个人数据统计
- 隐私政策与数据删除

禁止实现：

- 私聊、实时聊天、评论区
- 同城定位、附近的人
- 图片动态
- 付费、课程商城
- 多目标并行
- 复杂管理后台

## 目录约定

- `miniprogram/pages`：页面，只处理展示和交互。
- `miniprogram/config`：无密钥的客户端配置。
- `miniprogram/services`：后续新增的云函数调用封装。
- `miniprogram/utils`：无业务状态的通用工具。
- `cloudfunctions`：可信业务逻辑、身份校验、数据库写入和 AI 调用。

## 开发流程

1. 修改前先检查现有结构与 Git 状态。
2. 每次只实现一个功能模块，不顺手重构无关代码。
3. 开发前先说明计划。
4. 页面必须提供加载、空数据、失败和重试状态。
5. 完成后运行项目检查，并说明修改文件、实现方式、测试方法和已知问题。
6. 不覆盖或回退其他开发者的未提交修改。

## 数据与安全

- 客户端只提交业务输入，不提交可信身份字段。
- 云函数使用 `cloud.getWXContext()` 获取调用者身份。
- 集合权限遵循最小权限原则；敏感集合不允许客户端直接写入。
- 日志不得记录 AI 密钥、完整用户隐私数据或群二维码原图地址。
- 数据删除需要覆盖用户资料、目标、计划、打卡及队伍关联，并保留可审计的最少必要记录。

## UI 约定

- 主色为 `#356859`，背景色为 `#F5F7F2`。
- 复用 `app.wxss` 中的卡片、按钮和状态样式。
- 新页面需保持清晰、克制、低压力的表达。
- 不用仅靠颜色传递关键状态。
