# 2026-07-22

## AI 教练页「今日投入分布」卡片重设计
- 用户反馈旧的柱状图（rhythm-column 柱体）视觉差，要求换样式/功能图标。
- 改动范围：`miniprogram/pages/ai-coach/index.ts / .wxml / .wxss`（js 为脚手架占位，真实源码是 ts）。
- 新方案：顶部右侧显示「共 X 分钟」总量；一条横向水墨分段条（按各时段分钟数 flex-grow 占比铺开，墨绿四色 #9DBBAE/#7FA393/#4F7D6A/#245B4D，当前段金色 #C89B4A）；下方图标格——日视图用时段功能图标（上午 sun-rising / 下午 sunny / 晚上 sun-fall / 夜间 moon，均为 TDesign 图标），周/月/长期视图用墨点；无数据时段显示「—」并淡化图标，全空时显示虚线占位框+引导文案。
- 数据结构：RhythmPoint 去掉 height，新增 color/tint/icon；normalizeHeights 替换为 decorateRhythm；data 新增 rhythmSegments（分钟>0 的段）与 rhythmTotalMinutes。
- 已运行 `tsc --noEmit -p tsconfig.json` 通过；四个 scope（day/week/month/overall）共用同一组件，样式随点数自适应。
