# 2026-07-18 工作日志

## Bug 专项复查（未改代码）

对进步局小程序做上线前 Bug 专项复查，聚焦可复现逻辑 Bug 与运行时崩溃，覆盖前端 + 云函数。

**产出**：`docs/Bug复查报告.md`（42 个 Bug，含文件路径/行号/代码片段/复现条件/修复建议）

**核心发现**：
- P0 严重 5 项：
  1. date.js 的 TEST_REVIEW_OFFSET_DAYS=8 未归零（测试常量遗留，复盘入口提前8天）
  2. v2 userId 不一致，checkin 仍用 legacy stableId 写 users 表，streakDays 写错文档
  3. where().remove() 单次 20 条限制导致级联删除残留孤儿数据
  4. deleteUserData 不清理 v2 schema 的 team_members，注销后残留小队成员
  5. displayTaskTitle 把纯数字标题替换成示例文案（开发态遗留）
- P1 高 8 项、P2 中 21 项、P3 低 8 项

**关键判断**：P0-1 和 P0-5 是"开发态遗留进生产"，各改1行代码但影响面大；P0-2/3/4 是 v2 账号迁移不彻底，需系统性修复。
