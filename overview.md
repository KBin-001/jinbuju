# 上线前风险评估 - 概览

## 任务
对进步局（jinbuju）微信小程序进行全面的上线前风险评估，不改代码，覆盖安全、性能、代码规范、平台合规四个维度。

## 交付物
- **主报告**：`docs/上线前风险评估报告.md`（详细风险点、文件行号、改进建议）
- **本概览**：`overview.md`

## 核心发现（共 48 个风险点）

### P0 严重（8 项，阻塞上线）
1. `quickstartFunctions` 云函数完全无鉴权（云开发模板遗留）
2. 缺少 `privacy.json` 隐私协议保护指引
3. `app.json` 未声明 `requiredPrivateInfos`（chooseAvatar/getPhoneNumber）
4. UGC 完全缺失 msgSecCheck/imgSecCheck 内容安全检测
5. 隐私政策/用户协议为"候选稿"状态，缺运营主体等法定信息
6. 31 页全在主包，估算 3.5-4MB，超 2MB 限制无法上传
7. 约 607KB 未使用图片被打包
8. 打卡提交失败清空草稿导致用户无法重试（严重 Bug）

### P1 高（13 项，上线前尽量修）
- 头像上传无校验 + 未接入 imgSecCheck
- AI 生成内容未做安全检测
- 未调用 wx.requirePrivacyAuthorize
- 未成年人保护机制缺失
- onPageScroll 高频 setData（index、team）
- 热力图点击重传 371 cell 数组
- 50+ image 标签未用 lazy-load
- team 页 onShow 串行调 3-4 个云函数
- 大图未压缩
- 小队"一键完成"乐观更新失败无回滚
- 云环境 ID 硬编码
- 时长选项跨页不一致
- confirmColor 硬编码不统一

### P2 中（19 项）/ P3 低（8 项）
见主报告第四节、第五节。

## 关键结论
当前最大上线阻塞点不在代码质量（代码质量整体良好），而在**合规配置缺失**和**包体超限**。建议：
1. 1-2 天集中清掉 P0 合规与包体问题
2. 2-3 天处理 P1 安全检测与性能优化
3. 即可具备提审条件

## 做得好的方面
openid 来源正确、资源归属校验完善、小队权限分级、手机号用官方 API、输入校验全面、无外部 HTTP 请求、wx:for key 使用良好、CSS 动画、定时器清理到位、服务层统一错误处理、日期时区处理、防重复点击、无 @ts-ignore、账号注销功能完整。

## 后续建议
- 优先按 P0 清单逐项修复
- 上线前必须在云开发控制台核查数据库集合权限（无法通过代码审查确认）
- 完成隐私协议正式版后再提交审核
