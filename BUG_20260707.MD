我来帮你系统地排查项目中的 bug。先了解项目结构和关键代码,然后按 P0-P3 优先级列出发现的问题。

继续深入排查更多关键代码。

继续深入排查页面交互相关的代码。

现在开始汇总所有发现的 bug。让我继续检查一些边界情况。

现在我已经对项目进行了全面的代码审查,发现了多个 bug。让我汇总并按优先级整理。

# Bug 汇总报告

根据对今日进度｜目标计划打卡小程序项目的全面代码审查,我已发现以下 bug,按优先级 P0-P3 分类:

---

## P0 严重 bug(阻塞性问题)

### 1. ✅ 顺延任务日期计算错误（已修复）
**位置:** `miniprogram/services/manualTask.ts:95-108`
```typescript
export function rescheduleTask(taskId: string): ActionTask {
  // ...
  const today = getTodayBusinessDate();
  const baseDate = task.currentDate > today ? task.currentDate : today;
  task.currentDate = formatDate(addDays(new Date(`${baseDate}T00:00:00`), 1));
  // ...
}
```
**问题:** 顺延任务时基于 `task.currentDate` +1 而非今天 +1，且未使用 `getTodayBusinessDate()` 保证时区一致。过去任务顺延后仍在过去，与 UI 提示"已顺延到明天"不符。  
**影响:** 过期任务点击"顺延到明天"后日期仍在过去，无法正确顺延到明天。  
**复现:** 在1月31日创建任务,点击顺延,查看任务日期。  
**修复:** 使用 `getTodayBusinessDate()` 获取今天，对今天/过去任务顺延到明天(today+1)，对未来任务仍从 currentDate+1，避免往回移。跨月/跨年由 `addDays` 正确处理。

---

### 2. ✅ 连续打卡天数统计错误（已修复）
**位置:** `miniprogram/services/manualStats.ts:15-26`
```typescript
// 行动天数：completed 或 partially_completed 都算有行动记录
const actionDates = new Set(tasks.filter((task) => task.status === "completed" || task.status === "partially_completed").map((task) => task.currentDate));
// 连续打卡天数：只有 completed 才算真正打卡完成，partially_completed 不计入连续打卡
const streakDates = new Set(tasks.filter((task) => task.status === "completed").map((task) => task.currentDate));
let currentStreakDays = 0;
for (let offset = 0; offset > -365; offset -= 1) {
  const date = formatDate(addDays(new Date(`${today}T00:00:00`), offset));
  if (!streakDates.has(date)) break;
  currentStreakDays += 1;
}
```
**问题:** `actionDates` 包含 `completed` 和 `partially_completed` 状态任务的日期,但今天如果有 `partially_completed` 状态,也会被计入打卡天数,导致连续打卡统计不准确。  
**影响:** 进度页显示的连续打卡天数可能虚高。  
**复现:** 创建任务,标记为"完成一部分",查看进度页连续天数。  
**修复:** 将 `actionDates` 拆分为两个集合：`actionDates`（completed + partially_completed）用于行动天数统计，`streakDates`（仅 completed）用于连续打卡天数计算。

---

### 3. ✅ 今日页完成度计算错误（已修复）
**位置:** `miniprogram/pages/index/index.ts:39-44`
```typescript
function completionPercent(summary: TodaySummary): number {
  if (!summary.totalCount) return 0;
  // completed 计为 1，partially_completed 计为 0.5，体现部分进度
  const weighted = summary.completedCount + summary.partialCount * 0.5;
  return Math.round(weighted / summary.totalCount * 100);
}
```
**问题:** 只计算 `completed` 任务,未考虑 `partially_completed` 也应计入完成度。根据业务逻辑,"完成一部分"也应算作部分进度。  
**影响:** 今日页完成度偏低,不符合用户预期。  
**复现:** 创建3个任务,完成1个,部分完成1个,查看完成度显示。  
**修复:** `completionPercent` 改为加权计算：completed 权重 1，partially_completed 权重 0.5。同步更新 `progressSegments`（completed + partially_completed 都标记为 active）和 `todayMood`（新增 completedCount=0 但 partialCount>0 的文案分支）。

---

### 4. ✅ 热力图完成率计算逻辑错误（已修复）
**位置:** `miniprogram/services/manualStats.ts:34`
```typescript
// 热力图完成率：completed 权重 1，partially_completed 权重 0.5
const completionRate = dayTasks.length ? Math.round(((dayCompleted + dayPartial * 0.5) / dayTasks.length) * 100) : 0;
```
**问题:** 热力图的完成率只考虑 `completed` 任务,未考虑 `partially_completed`。如果一个任务部分完成,热力图显示0%完成率,不符合实际。  
**影响:** 进度页热力图数据不准确,无法真实反映当日完成情况。  
**复现:** 创建任务,标记为"完成一部分",查看热力图颜色。  
**修复:** 热力图 `completionRate` 改为加权计算：completed 权重 1，partially_completed 权重 0.5，与今日页 `completionPercent` 逻辑保持一致。

---

## P1 重要 bug(功能缺陷)

### 5. ✅ 任务状态回退逻辑缺失（已修复）
**位置:** `miniprogram/pages/index/index.ts:566-572`
```typescript
// 取消完成时：如果之前有 issueReason（曾标记为完成一部分），回退到 partially_completed
// 否则回退到 pending（待开始）
const nextStatus: ActionTaskStatus = task.status === "completed"
  ? (task.issueReason ? "partially_completed" : "pending")
  : "completed";
const nextActual = nextStatus === "completed" ? task.actualMinutes || task.estimatedMinutes : task.actualMinutes;
```
**问题:** 点击完成按钮可以取消完成状态,但会将任务状态直接改为 `pending`。如果任务是"完成一部分"状态后完成,取消完成应恢复为"完成一部分"而非"待开始"。  
**影响:** 用户误操作取消完成后,状态恢复不正确。  
**复现:** 将任务标记为"完成一部分",再标记完成,然后取消完成,查看状态。  
**修复:** `toggleTaskDone` 中取消完成时检查 `task.issueReason`：有值说明曾标记为"完成一部分"，回退到 `partially_completed` 并保留 `actualMinutes`；无值则回退到 `pending`。

---

### 6. ✅ getTasksByDate 排序逻辑可能不稳定（已修复）
**位置:** `miniprogram/services/manualTask.ts:49`
```typescript
export function getTasksByDate(goalId: string, date: string): ActionTask[] {
  return readManualStore().tasks.filter(...).sort((a, b) => a.createdAt.localeCompare(b.createdAt) || a.id.localeCompare(b.id));
}
```
**问题:** 仅按 `createdAt` 排序,当创建时间相同(如批量创建)时,排序顺序可能不稳定,影响任务展示顺序。  
**影响:** 今日任务顺序可能不一致,影响用户体验。  
**复现:** 快速添加多个任务,查看任务顺序。  
**修复:** 添加次要排序键 `a.id.localeCompare(b.id)`，当 `createdAt` 相同时按任务 ID 排序，确保排序结果稳定。

---

### 7. ✅ archiveGoal 时 activeGoalId 可能未重置（已修复）
**位置:** `miniprogram/services/manualGoal.ts:156-161`
```typescript
if (store.activeGoalId === goalId) {
  const nextActive = sortActiveGoals(store.goals).find((item) => item.id !== goalId);
  store.activeGoalId = nextActive?.id || undefined;
}
writeManualStore(store);
emit("goal:focus:update", { goalId: store.activeGoalId || "" });
```
**问题:** 如果存档的是最后一个活跃目标,`nextActive?.id` 可能是 `undefined`,导致 `activeGoalId` 被设为 `undefined` 字符串而非保持为空。  
**影响:** `getActiveGoal()` 可能在没有目标时返回错误的默认值。  
**复现:** 创建一个目标,归档该目标,再查看今日页。  
**修复:** 显式设为 `undefined` 并在 emit 时回退为空字符串，避免传递 `undefined` 给事件监听器。

---

### 8. ✅ 今日数据详情页边界计算可能为空（已修复）
**位置:** `miniprogram/services/todayDetails.ts:149-156`
```typescript
export function getTodayDataBounds(goalId: string, today: string): { minDate: string; maxDate: string } {
  const dates = getTasksByGoal(goalId).map((task) => task.currentDate).filter((date) => date <= today).sort();
  if (dates.length === 0) {
    // 没有历史任务时，默认展示最近 30 天范围
    const minDate = formatDate(addDays(new Date(`${today}T00:00:00`), -29));
    return { minDate, maxDate: today };
  }
  return { minDate: dates[0], maxDate: today };
}
```
**问题:** 如果所有任务的 `currentDate` 都大于 `today`,`dates` 数组为空,`minDate` 会是 `today`,但实际没有历史数据,导致日历显示异常。  
**影响:** 今日数据详情页的日历范围可能不正确。  
**复现:** 只创建未来的任务,打开今日数据详情页。  
**修复:** 当没有历史任务时，`minDate` 回退到 29 天前，保证日历有合理的展示范围。

---

## P2 次要 bug(体验问题)

### 9. ✅ 进度页热力图月份标签可能显示错误（已修复）
**位置:** `miniprogram/services/manualStats.ts:44-53` + `miniprogram/types/manual.ts:57-67`
```typescript
// GrowthHeatmapDay 新增 monthLabel?: string 字段
// manualStats.ts 中为每周第一天设置 monthLabel
for (let index = 0; index < heatmapDays.length; index += 7) {
  const week = heatmapDays.slice(index, index + 7);
  if (week.length > 0) {
    const firstDay = week[0];
    const monthNum = Number(firstDay.date.slice(5, 7));
    firstDay.monthLabel = `${monthNum}月`;
  }
  heatmapWeeks.push(week);
}
```
**问题:** `heatmapDays` 是按日期倒序排列的,但 `heatmapWeeks` 是每7天分组,月份边界可能在组内,导致月份标签不清晰。  
**影响:** 进度页热力图的月份分组显示可能不够直观。  
**复现:** 查看跨月份的热力图显示。  
**修复:** 在 `GrowthHeatmapDay` 类型新增 `monthLabel` 字段，在分组时为每周第一天计算所属月份，UI 可据此在周边界显示月份标签。

---

### 10. ✅ 小队成员任务详情缺少实际分钟数（已修复）
**位置:** `miniprogram/services/team.ts:146-148` + `miniprogram/types/team.ts:51-58`
```typescript
// TeamMemberActionDetail 新增 actualMinutes?: number 字段
function detail(id: string, title: string, status: MemberTodayStatus, estimatedMinutes: number, growthMinutes: number): TeamMemberActionDetail {
  return { id, title, status, estimatedMinutes, growthMinutes, actualMinutes: growthMinutes };
}
```
**问题:** `growthMinutes` 与 `actualMinutes` 语义不统一。任务详情使用 `growthMinutes`,但实际应该是 `actualMinutes`。  
**影响:** 小队成员任务详情的投入时间显示可能不准确。  
**复现:** 查看小队成员今日任务详情。  
**修复:** 在 `TeamMemberActionDetail` 类型新增 `actualMinutes` 字段，`detail()` 函数同时设置 `actualMinutes` 和 `growthMinutes`（值相同），UI 可优先使用 `actualMinutes` 显示实际投入时间。

---

### 11. ✅ TodayDataDetails 月份范围判断逻辑（已修复）
**位置:** `miniprogram/services/todayDetails.ts:102-106`
```typescript
const [startDate, endText] = item.date.split("|");
// 用 slice(0, 7) 取 "YYYY-MM"，再拼上 "-DD"，避免 slice(0, 8) 含末尾横杠的脆弱写法
const endDate = `${startDate.slice(0, 7)}-${String(endText).padStart(2, "0")}`;
```
**问题:** `dateRange` 函数构造的日期格式如 `"2026-01-01|5"`,拼接时 `slice(0, 8)` 包含了日期中的横杠,会导致 `endDate` 格式错误。  
**影响:** 月视图的数据范围计算可能不正确。  
**复现:** 选择月视图,查看数据分布。  
**修复:** 改用 `slice(0, 7)` 取 `"YYYY-MM"`，再显式拼 `"-DD"`，逻辑清晰且避免含末尾横杠的脆弱写法。

---

### 12. ✅ 任务展示状态 tone 不一致（已修复）
**位置:** `miniprogram/utils/taskStatus.ts:107`
```typescript
return { text: "待开始", tone: "neutral" };
```
**问题:** "待开始"状态使用 `success` 主题色,但"待开始"表示未完成,使用 `success` 可能误导用户。  
**影响:** UI 颜色语义与状态含义不匹配。  
**复现:** 查看今日页任务列表的颜色。  
**修复:** 将 "待开始" 的 tone 从 `success` 改为 `neutral`，与未完成的语义一致。

---

## P3 低优先级(代码质量问题)

### 13. ✅ createTask 时缺少 plannedDate 验证（已修复）
**位置:** `miniprogram/services/manualTask.ts:11-13`
```typescript
// 验证真实日历日期，防止 "2026-02-30" 等无效日期通过正则
const parsed = new Date(`${input.currentDate}T00:00:00`);
if (Number.isNaN(parsed.getTime())) throw new Error("请选择有效日期");
const [y, m, d] = input.currentDate.split("-").map(Number);
if (parsed.getFullYear() !== y || parsed.getMonth() + 1 !== m || parsed.getDate() !== d) throw new Error("请选择有效日期");
```
**问题:** `plannedDate` 直接使用 `input.currentDate`,未验证日期有效性(如2月30日)。  
**影响:** 如果输入无效日期,可能导致后续计算异常。  
**复现:** 尝试创建日期为"2026-02-30"的任务。  
**修复:** 在 `validate` 中新增日历日期校验：用 `new Date` 解析后比对年月日是否与输入一致，无效日期（如 2月30日）会被拦截。

---

### 14. ✅ updateTaskStatus 时未验证 actualMinutes（已修复）
**位置:** `miniprogram/services/manualTask.ts:69-72`
```typescript
if (actualMinutes !== undefined) {
  if (!Number.isInteger(actualMinutes)) throw new Error("实际时间请输入整数分钟");
  if (actualMinutes < 1 || actualMinutes > 480) throw new Error("实际时间应在 1～480 分钟之间");
}
```
**问题:** 验证正确,但错误提示不够友好,应区分整数验证和范围验证。  
**影响:** 用户体验,错误提示不够清晰。  
**复现:** 输入非整数或超出范围的分钟数。  
**修复:** 将整数验证和范围验证拆分为两个独立的 `if` 分支，分别给出针对性的错误提示。

---

### 15. ✅ groupTodayTasks 未处理 status 为 undefined 的情况（已修复）
**位置:** `miniprogram/utils/taskStatus.ts:142-149`
```typescript
const status = String(task.status || "pending") as ActionTaskStatus;
// 明确校验合法状态值，防止异常数据导致分组错误
const validStatuses: ActionTaskStatus[] = ["pending", "completed", "partially_completed", "skipped", "rescheduled"];
if (!validStatuses.includes(status)) continue;
if (status === "pending" || status === "partially_completed") {
  continueTasks.push(task);
}
```
**问题:** 虽然 `task.status || "pending"` 已处理,但应该明确检查是否为合法的状态值。  
**影响:** 如果数据异常,可能导致状态判断不正确。  
**复现:** 手动修改本地数据,设置无效状态值。  
**修复:** 新增 `validStatuses` 白名单校验，非法状态值直接跳过，防止异常数据导致分组错误。

---

### 16. ✅ calculateTodaySummary 计数逻辑可优化（已修复）
**位置:** `miniprogram/services/manualTask.ts:118-129`
```typescript
const summary = tasks.reduce((acc, task) => {
  acc.totalCount += 1;
  acc.estimatedMinutes += task.estimatedMinutes;
  acc.actualMinutes += task.actualMinutes || 0;
  if (task.status === "completed") acc.completedCount += 1;
  else if (task.status === "partially_completed") acc.partialCount += 1;
  return acc;
}, { estimatedMinutes: 0, actualMinutes: 0, completedCount: 0, partialCount: 0, totalCount: 0 });
// unfinishedCount = 总数 - 已完成 - 完成一部分，避免 else 兜底将异常状态误计为未完成
summary.unfinishedCount = summary.totalCount - summary.completedCount - summary.partialCount;
return summary as TodaySummary;
```
**问题:** `unfinishedCount` 应该是 `totalCount - completedCount - partialCount`,当前逻辑可能遗漏某些状态。  
**影响:** 统计数据可能不准确。  
**复现:** 测试包含各种状态的任务汇总。  
**修复:** 移除 reduce 中的 `else` 兜底分支，改为在 reduce 后用 `totalCount - completedCount - partialCount` 计算 `unfinishedCount`，避免异常状态被误计。

---

### 17. ✅ getTodayBusinessDate 时区处理（已修复）
**位置:** `miniprogram/utils/date.ts:13-23`
```typescript
export function getTodayBusinessDate(now: Date = new Date()): string {
  const shanghaiDate = new Date(now.getTime() + SHANGHAI_OFFSET_MILLISECONDS);
  const year = shanghaiDate.getUTCFullYear();
  const month = shanghaiDate.getUTCMonth() + 1;
  const day = shanghaiDate.getUTCDate();
  // 校验构造的日期是否合法，防止时区转换临界时刻产生非法日期
  const verify = new Date(Date.UTC(year, month - 1, day));
  if (verify.getUTCFullYear() !== year || verify.getUTCMonth() + 1 !== month || verify.getUTCDate() !== day) {
    return `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}`;
  }
  return `${year}-${pad(month)}-${pad(day)}`;
}
```
**问题:** 使用 UTC 方法获取日期,在跨午夜边缘可能产生边界问题。  
**影响:** 在时区转换临界时刻可能返回错误的日期。  
**复现:** 在23:59-00:01时间段测试日期获取。  
**修复:** 新增 `Date.UTC` 反向校验：构造后验证年月日是否一致，不一致时回退到本地日期，保证结果始终合法。

---

### 18. ✅ buildStats 统计逻辑未过滤特定状态（已修复）
**位置:** `miniprogram/services/manualGoal.ts:35-38`
```typescript
// 过滤掉 rescheduled（已顺延）和 skipped（今天不做），它们不应计入可见行动总数
const visibleActions = actions.filter((task) => task.status !== "rescheduled" && task.status !== "skipped");
```
**问题:** `totalActions` 包含所有非 `rescheduled` 状态的任务,但 `skipped` 状态的任务不应计入"可见行动"。  
**影响:** 归档目标的完成率计算可能偏低。  
**复现:** 创建已归档目标,包含"今天不做"状态的任务,查看完成率。  
**修复:** `visibleActions` 过滤条件新增 `task.status !== "skipped"`，将"今天不做"的任务排除在可见行动总数之外，使完成率计算更准确。

---

## 总结

| 优先级 | 数量 | 类型 |
|--------|------|------|
| P0 | 4 | 阻塞性问题,影响核心功能 |
| P1 | 4 | 功能缺陷,影响正常使用 |
| P2 | 4 | 体验问题,影响用户感知 |
| P3 | 5 | 代码质量问题,影响维护性 |

**建议修复顺序:** P0 → P1 → P2 → P3

P0 级 bug 涉及日期计算、统计等核心逻辑,应优先修复。P1 级 bug 影响状态管理,建议尽快解决。P2/P3 级可根据产品规划逐步优化。