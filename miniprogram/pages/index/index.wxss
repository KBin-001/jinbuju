/* =========================================================
 * 今日页 · 浅绿成长风
 * --------------------------------------------------------
 * 设计方向：浅绿、白卡片、轻阴影、极少量深绿
 * 关键决策：Hero 改白卡 + 极浅薄荷绿，Action Panel 顶部用浅绿渐变
 * ========================================================= */

@import "../../styles/theme.wxss";

/* ============ 墨绿成长主题 覆写 ============
 * 由于本页 WXSS 中存在大量硬编码的薄荷绿颜色（rgba/hex/gradient），
 * 这些值在切换到墨绿主题后不会自动跟随 CSS 变量。
 * 因此在切换至墨绿主题时，对最显眼的关键硬编码值做精细化覆写：
 *  · 进度条渐变（active / fill）
 *  · 卡片进度滑轨（rail-success / rail-primary / rail-warning）
 *  · 完成图标阴影
 *  · 心情、提示语等次级色
 * 其余依赖 CSS 变量的样式已被 theme.wxss 覆盖。
 * ---------------------------------------------------------- */
.today-page[data-theme="inkGreen"] {
  --today-primary: #1F6F55;
  --today-primary-deep: #0F3F32;
  --today-accent: #6DBB95;
}

.today-page[data-theme="inkGreen"] .today-hero::after {
  background: rgba(31, 111, 85, 0.20);
}

.today-page[data-theme="inkGreen"] .hero-progress-segment {
  background: rgba(109, 187, 149, 0.32);
}

.today-page[data-theme="inkGreen"] .hero-progress-segment.is-active {
  background: linear-gradient(90deg, #6DBB95 0%, #1F6F55 100%);
}

.today-page[data-theme="inkGreen"] .hero-progress-fill {
  background: linear-gradient(90deg, #6DBB95 0%, #1F6F55 100%);
}

.today-page[data-theme="inkGreen"] .progress-ring {
  background: conic-gradient(#6DBB95 var(--ring-deg, 0%), rgba(109, 187, 149, 0.18) 0);
}

.today-page[data-theme="inkGreen"] .week-switch,
.today-page[data-theme="inkGreen"] .week-day.is-today .week-number,
.today-page[data-theme="inkGreen"] .week-day.is-selected .week-number {
  box-shadow: 0 8rpx 18rpx rgba(31, 111, 85, 0.28);
}

.today-page[data-theme="inkGreen"] .rail-success { background: linear-gradient(180deg, #6DBB95 0%, #1F6F55 100%); }
.today-page[data-theme="inkGreen"] .rail-warning { background: linear-gradient(180deg, #F4C95D 0%, #E0A832 100%); }
.today-page[data-theme="inkGreen"] .rail-primary,
.today-page[data-theme="inkGreen"] .rail-neutral,
.today-page[data-theme="inkGreen"] .rail-muted { background: linear-gradient(180deg, #C8E0D4 0%, #6DBB95 100%); }

.today-page[data-theme="inkGreen"] .action-card-completed {
  border-color: rgba(66, 166, 122, 0.30);
}

.today-page[data-theme="inkGreen"] .action-card-completed .action-title {
  text-decoration-color: rgba(66, 166, 122, 0.42);
}

.today-page[data-theme="inkGreen"] .action-completed-icon,
.today-page[data-theme="inkGreen"] .today-bottom-action,
.today-page[data-theme="inkGreen"] .quick-add-primary,
.today-page[data-theme="inkGreen"] .calendar-confirm,
.today-page[data-theme="inkGreen"] .quick-duration-check {
  box-shadow: 0 8rpx 24rpx rgba(31, 111, 85, 0.28);
}

.today-page[data-theme="inkGreen"] .today-data-mood {
  color: #B08A35;
}

.today-page[data-theme="inkGreen"] .daily-nudge-label,
.today-page[data-theme="inkGreen"] .today-data-tag {
  color: #8A6B1F;
}

.today-page[data-theme="inkGreen"] .quick-add-mask,
.today-page[data-theme="inkGreen"] .quick-duration-layer {
  background: rgba(15, 63, 50, 0.36);
}

.today-page[data-theme="inkGreen"] .calendar-mask {
  background: rgba(15, 63, 50, 0.18);
}

.today-page[data-theme="inkGreen"] .calendar-day.is-outside {
  color: #B6C7C0;
}

/* 展开日历选中态：与今日页内联周日历保持一致的黄色，避免旧模板绿色选中态 */
.today-page[data-theme="inkGreen"] .calendar-day.is-selected .calendar-day-number {
  background: #F4C95D;
  color: #2F4A3F;
  font-weight: 800;
  box-shadow: 0 8rpx 18rpx rgba(244, 201, 93, 0.28);
}

/* 今日但未被选中：浅绿柔和标记，与内联周日历一致 */
.today-page[data-theme="inkGreen"] .calendar-day.is-today:not(.is-selected) .calendar-day-number {
  background: #EAF5EF;
  color: #356859;
}

.today-page {
  --today-primary: var(--color-primary);
  --today-primary-deep: var(--color-primary-deep);
  --today-accent: var(--color-primary-light);
  --today-gold: var(--color-warning);
  --today-text: var(--color-text-main);
  --today-secondary: var(--color-text-secondary);
  --today-muted: var(--color-text-muted);
  --today-border: var(--color-border);
  --today-shadow: var(--shadow-card);
  --today-shadow-soft: var(--shadow-card-soft);
  padding: 28rpx 32rpx calc(166rpx + env(safe-area-inset-bottom));
  background: var(--color-bg);
  min-height: 100vh;
}

/* ============ 顶部问候 ============ */
.today-topbar {
  min-height: 104rpx;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
  margin-bottom: 10rpx;
  padding: 22rpx 178rpx 10rpx 0;
}

.today-greeting {
  color: var(--color-text-main);
  font-size: 32rpx;
  line-height: 42rpx;
  font-weight: 800;
  letter-spacing: 0;
}

.today-subtitle {
  margin-top: 4rpx;
  color: var(--color-text-muted);
  font-size: 23rpx;
  line-height: 32rpx;
}

.today-mini-date {
  min-height: 48rpx;
  display: inline-flex;
  align-items: center;
  flex: none;
  padding: 0 18rpx;
  border-radius: 24rpx;
  background: var(--color-card);
  color: var(--color-primary);
  font-size: 22rpx;
  font-weight: 700;
  box-shadow: var(--shadow-card-soft);
}

/* ============ Hero（白卡 + 极浅薄荷渐变，不再大面积深绿） ============ */
.today-hero {
  margin-bottom: 24rpx;
  padding: 32rpx 32rpx 30rpx;
  border-radius: 32rpx;
  background: var(--gradient-card-mint);
  color: var(--color-text-main);
  box-shadow: var(--shadow-card);
  position: relative;
  overflow: hidden;
}

.today-hero::after {
  content: "";
  position: absolute;
  right: -60rpx;
  top: -60rpx;
  width: 220rpx;
  height: 220rpx;
  border-radius: 50%;
  background: rgba(124, 198, 160, 0.22);
  pointer-events: none;
}

.hero-topline {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
  z-index: 1;
}

.hero-kicker {
  position: relative;
  color: var(--color-primary);
  font-size: 20rpx;
  font-weight: 700;
  letter-spacing: 3rpx;
}

.hero-date-pill {
  min-height: 42rpx;
  display: inline-flex;
  align-items: center;
  flex: none;
  box-sizing: border-box;
  padding: 0 16rpx;
  border: 1rpx solid var(--color-border);
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.7);
  color: var(--color-primary-deep);
  font-size: 21rpx;
  font-weight: 600;
  white-space: nowrap;
}

.hero-focus {
  position: relative;
  margin-top: 24rpx;
  z-index: 1;
}

.hero-day-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.hero-day {
  color: var(--color-text-secondary);
  font-size: 28rpx;
  font-weight: 800;
  letter-spacing: 2rpx;
  line-height: 1;
}

.hero-status-pill {
  min-height: 36rpx;
  display: inline-flex;
  align-items: center;
  box-sizing: border-box;
  padding: 0 14rpx;
  border: 1rpx solid var(--color-border);
  border-radius: 999rpx;
  background: var(--color-card);
  color: var(--color-primary);
  font-size: 20rpx;
  font-weight: 650;
  line-height: 1;
}

.hero-focus-title {
  max-width: 520rpx;
  display: -webkit-box;
  overflow: hidden;
  margin-top: 10rpx;
  color: var(--color-text-main);
  font-size: 42rpx;
  font-weight: 800;
  line-height: 1.16;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.weekday {
  margin-top: 6rpx;
  color: var(--color-text-secondary);
  font-size: 24rpx;
}

.hero-copy {
  position: relative;
  margin-top: 18rpx;
  color: var(--color-text-secondary);
  font-size: 25rpx;
  line-height: 1.5;
  z-index: 1;
}

.hero-progress-badge {
  min-width: 76rpx;
  height: 46rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex: none;
  box-sizing: border-box;
  padding: 0 18rpx;
  border: 1rpx solid var(--color-border);
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.7);
  color: var(--color-primary-deep);
  font-size: 24rpx;
  font-weight: 600;
  line-height: 1;
}

.hero-progress {
  position: relative;
  margin-top: 22rpx;
  z-index: 1;
}

.hero-progress-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  margin-bottom: 10rpx;
  color: var(--color-text-secondary);
  font-size: 21rpx;
}

.hero-progress-track {
  width: 100%;
  height: 12rpx;
  display: flex;
  gap: 8rpx;
  overflow: visible;
  border-radius: 0;
  background: transparent;
}

.hero-progress-segment {
  height: 100%;
  flex: 1;
  min-width: 0;
  border-radius: 999rpx;
  background: rgba(124, 198, 160, 0.32);
}

.hero-progress-segment.is-active {
  background: linear-gradient(90deg, #9BD9B7 0%, #68BE8F 100%);
  animation: todayProgressFill 0.6s ease-out both;
}

.hero-progress-fill {
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, #7CC6A0 0%, #3F8F72 100%);
  animation: todayProgressFill 0.6s ease-out both;
  transition: width 180ms ease-out;
}

@keyframes todayProgressFill {
  from {
    transform: scaleX(0);
    transform-origin: left center;
  }
  to {
    transform: scaleX(1);
    transform-origin: left center;
  }
}

/* ============ 周历卡片 ============ */
.week-card {
  height: 184rpx;
  min-height: 184rpx;
  margin-top: 10rpx;
  padding: 22rpx 26rpx 18rpx;
  border-radius: 28rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-card);
  box-sizing: border-box;
}

.week-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  margin-bottom: 16rpx;
  height: 40rpx;
}

.week-title {
  color: var(--color-text-main);
  font-size: 30rpx;
  line-height: 40rpx;
  font-weight: 800;
}

.week-title text {
  margin-left: 10rpx;
  color: var(--color-text-secondary);
  font-size: 24rpx;
  font-weight: 600;
}

.week-switch {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  border-radius: 50%;
  background: var(--color-primary);
  color: #ffffff;
  font-size: 30rpx;
  box-shadow: 0 8rpx 18rpx rgba(63, 143, 114, 0.22);
}

.week-chevron {
  width: 12rpx;
  height: 12rpx;
  box-sizing: border-box;
  border-right: 3rpx solid #ffffff;
  border-bottom: 3rpx solid #ffffff;
  transform: translateY(-2rpx) rotate(45deg);
}

.week-switch:active {
  opacity: 0.86;
  transform: scale(0.96);
}

.week-grid {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 0;
}

.week-day {
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  height: 86rpx;
  justify-content: flex-start;
  color: var(--color-text-muted);
}

.week-label {
  height: 28rpx;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  line-height: 28rpx;
}

.week-number {
  width: 44rpx;
  height: 44rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  line-height: 44rpx;
  font-weight: 700;
}

.week-day.is-today .week-number {
  background: var(--color-primary);
  color: #ffffff;
  box-shadow: 0 6rpx 16rpx rgba(63, 143, 114, 0.22);
}

.week-day.is-today .week-label {
  color: var(--color-primary);
  font-weight: 800;
}

.week-day.is-selected .week-number {
  background: var(--color-primary);
  color: #ffffff;
  box-shadow: 0 8rpx 18rpx rgba(63, 143, 114, 0.24);
}

.week-day.is-selected .week-label {
  color: var(--color-primary);
  font-weight: 800;
}

.week-day.is-today:not(.is-selected) .week-number {
  background: var(--color-primary-soft);
  color: var(--color-primary);
  box-shadow: none;
}

.week-day.is-muted {
  opacity: 0.45;
}

.week-day.is-muted.is-selected {
  opacity: 1;
}

/* ============ 今日行动面板（顶部用浅绿渐变，主体白卡） ============ */
.today-action-panel {
  overflow: hidden;
  margin-top: 28rpx;
  border-radius: 28rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-card);
}

.today-action-hero {
  min-height: 180rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 28rpx;
  padding: 28rpx 30rpx;
  background:
    radial-gradient(circle at 80% 45%, rgba(255, 255, 255, 0.4), transparent 30%),
    var(--gradient-card-mint);
  color: var(--color-text-main);
}

.action-hero-label {
  color: var(--color-primary);
  font-size: 24rpx;
  line-height: 32rpx;
  font-weight: 700;
  letter-spacing: 2rpx;
}

.action-hero-count {
  display: flex;
  align-items: baseline;
  margin-top: 10rpx;
  color: var(--color-text-main);
}

.action-hero-count text:first-child {
  font-size: 50rpx;
  line-height: 58rpx;
  font-weight: 800;
}

.action-total {
  font-size: 34rpx;
  font-weight: 800;
}

.action-unit {
  margin-left: 10rpx;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  font-weight: 600;
}

.action-hero-goal {
  max-width: 376rpx;
  margin-top: 8rpx;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  line-height: 30rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 100% 圆环：使用 primary-light */
.progress-ring {
  width: 108rpx;
  height: 108rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  border-radius: 50%;
  background: conic-gradient(var(--color-primary-light) var(--ring-deg, 0%), rgba(124, 198, 160, 0.18) 0);
  transition: background 180ms ease-out;
}

.progress-ring-inner {
  width: 84rpx;
  height: 84rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--color-card);
  color: var(--color-primary-deep);
}

.progress-ring-inner view {
  font-size: 27rpx;
  line-height: 32rpx;
  font-weight: 800;
}

.progress-ring-inner text {
  margin-top: 4rpx;
  color: var(--color-text-secondary);
  font-size: 18rpx;
  line-height: 22rpx;
}

/* 任务列表 */
.today-action-list {
  padding: 20rpx 28rpx 26rpx;
  background: var(--color-card);
}

.today-task-row {
  min-height: 80rpx;
  display: flex;
  align-items: center;
  gap: 18rpx;
}

.today-task-row + .today-task-row {
  margin-top: 4rpx;
}

.task-box {
  width: 32rpx;
  height: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  border: 2rpx solid #c9d8d1;
  border-radius: 8rpx;
  color: #ffffff;
  font-size: 20rpx;
  transition: background 160ms ease-out, border-color 160ms ease-out, transform 160ms ease-out;
}

.today-task-row.is-done .task-box {
  border-color: var(--color-success);
  background: var(--color-success);
  transform: scale(1.02);
}

.task-copy {
  min-width: 0;
  flex: 1;
}

.task-title {
  color: var(--color-text-main);
  font-size: 28rpx;
  line-height: 36rpx;
  font-weight: 700;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.task-desc {
  margin-top: 2rpx;
  color: var(--color-text-muted);
  font-size: 22rpx;
  line-height: 30rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.today-task-row.is-done .task-title {
  color: var(--color-text-secondary);
}

.task-tail-check {
  width: 36rpx;
  height: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  color: var(--color-primary);
  font-size: 26rpx;
}

.action-list-toggle {
  min-height: 52rpx;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  margin: 8rpx 0 6rpx 50rpx;
  color: var(--color-text-muted);
  font-size: 24rpx;
  line-height: 1;
}

.action-empty-inline {
  padding: 8rpx 0 22rpx;
  color: var(--color-text-main);
  font-size: 28rpx;
  font-weight: 700;
}

.action-empty-inline text {
  display: block;
  margin-top: 8rpx;
  color: var(--color-text-muted);
  font-size: 24rpx;
  font-weight: 400;
}

.inline-add-action {
  width: 100%;
  height: 72rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
  box-sizing: border-box;
  margin-top: 8rpx;
  padding: 0 22rpx;
  border-radius: 20rpx;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  font-size: 26rpx;
  line-height: 34rpx;
  font-weight: 700;
}

/* ============ 今日统计卡 ============ */
.today-data-card {
  margin-top: 28rpx;
  padding: 26rpx 28rpx;
  border-radius: 28rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-card);
  box-sizing: border-box;
}

.today-data-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
}

.today-data-header > view:first-child {
  min-width: 0;
  flex: 1;
}

.today-data-title {
  color: var(--color-text-main);
  font-size: 28rpx;
  line-height: 36rpx;
  font-weight: 800;
}

.today-data-mood {
  margin-top: 6rpx;
  color: #B08A35;
  font-size: 23rpx;
  font-weight: 650;
  line-height: 1.35;
}

.today-data-badge {
  min-height: 42rpx;
  display: inline-flex;
  align-items: center;
  flex: none;
  box-sizing: border-box;
  padding: 0 16rpx;
  border-radius: 20rpx;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  font-size: 22rpx;
  font-weight: 650;
  white-space: nowrap;
}

.today-data-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 0;
  margin-top: 22rpx;
  border: 0;
}

.today-data-item {
  min-width: 0;
  min-height: 86rpx;
  padding: 0 8rpx;
  border: 0;
  border-radius: 0;
  background: transparent;
  text-align: center;
  box-sizing: border-box;
}

.today-data-item + .today-data-item {
  border-left: 1rpx solid var(--color-border-soft);
}

.today-data-value {
  color: var(--color-text-main);
  font-size: 40rpx;
  line-height: 48rpx;
  font-weight: 800;
}

.today-data-value text {
  margin-left: 4rpx;
  color: var(--color-text-main);
  font-size: 19rpx;
  font-weight: 700;
}

.today-data-efficiency {
  color: var(--color-primary-deep);
}

.today-data-label {
  margin-top: 6rpx;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  line-height: 30rpx;
}

.today-data-footer {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 22rpx;
  padding-top: 18rpx;
  border-top: 1rpx solid var(--color-border-soft);
}

.today-data-tag {
  min-height: 40rpx;
  display: inline-flex;
  align-items: center;
  box-sizing: border-box;
  padding: 0 14rpx;
  border-radius: 999rpx;
  background: var(--color-warning-soft);
  color: #8A6B1F;
  font-size: 22rpx;
  font-weight: 600;
}

/* ============ 心情 / 低压力卡 ============ */
.today-mood {
  display: flex;
  align-items: center;
  gap: 18rpx;
  margin-top: 20rpx;
  padding: 20rpx 22rpx;
  border-radius: 24rpx;
  background: var(--color-card-soft);
}

.today-mood-mark {
  width: 58rpx;
  height: 58rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  border-radius: 18rpx;
  background: var(--color-primary);
  color: #ffffff;
  font-size: 25rpx;
  font-weight: 800;
}

.today-mood-copy {
  min-width: 0;
  flex: 1;
}

.today-mood-title {
  color: var(--color-text-main);
  font-size: 29rpx;
  font-weight: 750;
  line-height: 1.25;
}

.today-mood-desc {
  margin-top: 6rpx;
  color: var(--color-text-secondary);
  font-size: 23rpx;
  line-height: 1.45;
}

.mood-done .today-mood-mark { background: var(--color-primary-deep); }
.mood-half .today-mood-mark { background: var(--color-primary-light); color: var(--color-primary-deep); }
.mood-low .today-mood-mark,
.mood-empty .today-mood-mark { background: var(--color-warning); color: #6B5418; }

/* ============ 每日小提示（黄底） ============ */
.daily-nudge-card {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 26rpx;
  padding: 22rpx 24rpx;
  border-radius: 24rpx;
  background: var(--color-warning-soft);
  box-shadow: none;
}

.daily-nudge-label {
  flex: none;
  color: #8A6B1F;
  font-size: 22rpx;
  font-weight: 750;
  line-height: 1.45;
}

.daily-nudge-text {
  min-width: 0;
  flex: 1;
  color: var(--color-text-secondary);
  font-size: 24rpx;
  line-height: 1.5;
}

.quote-mark {
  flex: none;
  color: #B7BDB8;
  font-size: 44rpx;
  line-height: 40rpx;
  font-weight: 800;
}

/* ============ 目标卡 / 段头 / 操作 ============ */
.goal-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  align-items: flex-start;
  margin-bottom: 22rpx;
  padding: 28rpx 30rpx;
  border-color: var(--color-border);
  border-radius: 28rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-card);
}

.goal-card > view:first-child {
  min-width: 0;
  flex: 1;
}

.eyebrow {
  color: var(--color-primary);
  font-size: 22rpx;
  font-weight: 650;
}

.goal-title {
  margin-top: 8rpx;
  color: var(--color-text-main);
  font-size: 33rpx;
  font-weight: 700;
  line-height: 1.35;
}

.goal-description {
  margin-top: 8rpx;
  color: var(--color-text-secondary);
  font-size: 24rpx;
  line-height: 1.5;
}

.goal-chip {
  min-height: 46rpx;
  display: inline-flex;
  align-items: center;
  flex: none;
  padding: 6rpx 16rpx;
  border-radius: 999rpx;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  font-size: 21rpx;
  font-weight: 650;
  white-space: nowrap;
}

.empty-card {
  margin-top: 24rpx;
  text-align: center;
}

.empty-mark {
  width: 90rpx;
  height: 90rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto;
  border-radius: 28rpx;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  font-weight: 700;
}

/* ============ 行动段头 / 添加按钮 ============ */
.action-section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  width: 100%;
  box-sizing: border-box;
  margin: 20rpx 0 16rpx;
}

.action-section-left {
  min-width: 0;
  flex: 1;
}

.action-section-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  flex-shrink: 0;
  min-width: 118rpx;
}

.section-title {
  min-width: 0;
  color: var(--color-text-main);
  font-size: 34rpx;
  line-height: 42rpx;
  font-weight: 700;
}

.section-caption {
  min-width: 0;
  margin-top: 6rpx;
  color: var(--color-text-muted);
  font-size: 24rpx;
  line-height: 1.35;
}

.section-count {
  flex: none;
  margin-top: 10rpx;
  color: var(--color-text-muted);
  font-size: 24rpx;
  line-height: 1.35;
  text-align: right;
  white-space: nowrap;
}

.add-action-button {
  min-height: 56rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
  flex: none;
  box-sizing: border-box;
  padding: 0 8rpx;
  border-radius: 0;
  background: transparent;
  color: var(--color-primary);
  font-size: 26rpx;
  font-weight: 700;
  line-height: 1;
}

.add-action-button:active {
  color: var(--color-primary-deep);
  opacity: 0.78;
}

/* ============ 行动任务卡片 ============ */
.task-group {
  margin-top: 22rpx;
}

.group-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 0 2rpx 14rpx;
}

.group-title {
  color: var(--color-text-main);
  font-size: 27rpx;
  font-weight: 700;
}

.group-count {
  color: var(--color-text-muted);
  font-size: 21rpx;
}

.action-card-tap {
  width: 100%;
  margin-bottom: 20rpx;
}

.action-card {
  position: relative;
  overflow: hidden;
  border: 1rpx solid var(--color-border);
  border-radius: 28rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-card);
}

.action-card .card-body {
  padding: 26rpx 28rpx;
}

.action-card-completed {
  border-color: rgba(104, 190, 143, 0.24);
  background: var(--color-card-hover);
}

.action-card-body {
  position: relative;
  display: flex;
  align-items: center;
  gap: 20rpx;
  min-height: 136rpx;
}

.action-status-rail {
  width: 10rpx;
  align-self: stretch;
  flex: none;
  border-radius: 999rpx;
  background: var(--color-primary-soft);
}

.rail-success { background: linear-gradient(180deg, #9BD9B7 0%, #68BE8F 100%); }
.rail-warning { background: linear-gradient(180deg, #F4C95D 0%, #E0A832 100%); }
.rail-primary,
.rail-neutral,
.rail-muted { background: linear-gradient(180deg, #C5E5D4 0%, #7CC6A0 100%); }

.action-main {
  min-width: 0;
  flex: 1;
}

.action-tags {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10rpx;
}

.status-tag,
.source-tag,
.partial-hint {
  height: 40rpx;
  padding: 0 18rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
}

.status-muted { --tag-primary-color: var(--color-text-secondary); --tag-primary-light-color: #EEF1ED; }
.status-neutral { --tag-primary-color: #46627A; --tag-primary-light-color: #E8F0F7; }

.action-title {
  margin-top: 12rpx;
  color: var(--color-text-main);
  font-size: 30rpx;
  line-height: 40rpx;
  font-weight: 700;
}

.action-card-completed .action-title {
  color: var(--color-text-secondary);
  text-decoration: line-through;
  text-decoration-color: rgba(104, 190, 143, 0.42);
  text-decoration-thickness: 2rpx;
}

.action-description {
  display: -webkit-box;
  overflow: hidden;
  margin-top: 8rpx;
  color: var(--color-text-secondary);
  font-size: 24rpx;
  line-height: 1.55;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.action-meta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 10rpx 18rpx;
  margin-top: 12rpx;
  color: var(--color-text-secondary);
  font-size: 22rpx;
}

.meta-item {
  display: flex;
  align-items: center;
  gap: 6rpx;
  white-space: nowrap;
}

.meta-label {
  color: var(--color-text-muted);
}

/* 完成按钮（小圆形） */
.action-complete-btn,
.action-completed-icon {
  width: 56rpx;
  min-width: 56rpx;
  max-width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
}

.action-complete-btn {
  border: 2rpx solid #C9D8D1;
  background: var(--color-card);
  color: transparent;
  font-size: 24rpx;
  font-weight: 600;
  line-height: 1;
}

.action-complete-btn::after { border: 0; }

.action-complete-btn:active {
  border-color: var(--color-primary);
  background: var(--color-primary-soft);
  transform: scale(0.98);
}

.action-completed-icon {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: #ffffff;
  font-size: 30rpx;
  box-shadow: 0 8rpx 20rpx rgba(63, 143, 114, 0.22);
}

.empty-action-card {
  text-align: center;
  border-radius: 28rpx;
  box-shadow: var(--shadow-card);
}

.empty-add-button {
  margin-top: 28rpx;
  border-radius: 999rpx;
}

/* ============ 底部操作 ============ */
.today-bottom-action {
  width: 100%;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 24rpx;
  border-radius: 44rpx;
  background: var(--color-primary);
  color: #ffffff;
  font-size: 30rpx;
  font-weight: 700;
  box-shadow: 0 10rpx 24rpx rgba(63, 143, 114, 0.22);
}

/* ============ 快速添加面板 ============ */
.quick-add-layer {
  position: fixed;
  inset: 0;
  z-index: 20;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  box-sizing: border-box;
}

.quick-add-mask {
  position: absolute;
  inset: 0;
  box-sizing: border-box;
  background: rgba(36, 76, 67, 0.32);
  backdrop-filter: blur(6rpx);
}

.quick-add-panel {
  position: relative;
  width: calc(100vw - 48rpx);
  max-width: 680rpx;
  max-height: calc(100vh - 180rpx);
  overflow-y: auto;
  box-sizing: border-box;
  margin-bottom: 24rpx;
  padding: 16rpx 28rpx calc(28rpx + env(safe-area-inset-bottom));
  border: 1rpx solid var(--color-border);
  border-radius: 32rpx;
  background: linear-gradient(180deg, var(--color-bg), var(--color-card) 250rpx);
  box-shadow: var(--shadow-float);
  animation: quick-add-enter 220ms ease-out both;
}

.quick-add-grabber {
  box-sizing: border-box;
  width: 70rpx;
  height: 7rpx;
  margin: 0 auto 18rpx;
  border-radius: 999rpx;
  background: var(--color-border);
}

.quick-add-header {
  display: flex;
  align-items: flex-start;
  justify-content: flex-start;
  width: 100%;
  box-sizing: border-box;
  padding-right: 72rpx;
}

.quick-add-header > view:first-child {
  min-width: 0;
  flex: 1;
}

.quick-add-kicker {
  color: var(--color-primary);
  font-size: 19rpx;
  font-weight: 700;
  letter-spacing: 3rpx;
}

.quick-add-title {
  margin-top: 6rpx;
  color: var(--color-text-main);
  font-size: 38rpx;
  font-weight: 750;
}

.quick-add-close {
  position: absolute;
  right: 32rpx;
  top: 32rpx;
  box-sizing: border-box;
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  background: var(--color-bg);
  color: var(--color-text-secondary);
  font-size: 30rpx;
  line-height: 1;
  z-index: 1;
}

.quick-add-close.is-disabled {
  opacity: 0.48;
  pointer-events: none;
}

.quick-add-close:active {
  background: var(--color-primary-soft);
  color: var(--color-primary);
}

.quick-add-copy {
  box-sizing: border-box;
  margin-top: 14rpx;
  color: var(--color-text-secondary);
  font-size: 24rpx;
  line-height: 1.6;
}

.quick-add-field {
  width: 100%;
  box-sizing: border-box;
  margin-top: 24rpx;
}

.quick-add-label {
  color: var(--color-text-main);
  font-size: 25rpx;
  font-weight: 700;
}

.quick-add-hint {
  margin-top: 5rpx;
  color: var(--color-text-muted);
  font-size: 22rpx;
}

.quick-add-input,
.quick-add-textarea {
  width: 100%;
  box-sizing: border-box;
  margin-top: 12rpx;
  border: 1rpx solid var(--color-border);
  border-radius: 24rpx;
  background: var(--color-card);
  color: var(--color-text-main);
  font-size: 28rpx;
}

.quick-add-input {
  height: 88rpx;
  padding: 0 24rpx;
}

.quick-add-textarea {
  height: 136rpx;
  padding: 20rpx 24rpx;
  line-height: 1.5;
}

.quick-add-placeholder {
  color: var(--color-text-muted);
}

.quick-add-minutes-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  width: 100%;
  box-sizing: border-box;
  padding: 22rpx 24rpx;
  border-radius: 24rpx;
  border: 1rpx solid var(--color-border);
  background: var(--color-card-soft);
}

.quick-add-minutes-row > view:first-child {
  min-width: 0;
  flex: 1;
}

.quick-add-minutes-control {
  width: 220rpx;
  max-width: 100%;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  flex: none;
  margin-top: 0;
  padding: 0 20rpx;
  box-sizing: border-box;
  border: 1rpx solid var(--color-border);
  border-radius: 24rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-card-soft);
}

.quick-add-minutes-control:active {
  background: var(--color-card-hover);
  box-shadow: var(--shadow-card-soft);
}

.quick-add-minutes-value {
  color: var(--color-text-main);
  font-size: 35rpx;
  font-weight: 750;
}

.quick-add-minute-unit {
  flex: none;
  color: var(--color-text-secondary);
  font-size: 23rpx;
}

.quick-add-picker-arrow {
  margin-left: 4rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  color: var(--color-text-secondary);
  font-size: 22rpx;
  line-height: 1;
}

.quick-add-actions {
  display: flex;
  gap: 20rpx;
  width: 100%;
  box-sizing: border-box;
  margin-top: 26rpx;
}

.quick-add-secondary,
.quick-add-primary {
  flex: 1;
  width: auto;
  min-width: 0;
  min-height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 700;
}

.quick-add-secondary {
  border: 2rpx solid var(--color-border);
  background: var(--color-card);
  color: var(--color-primary);
}

.quick-add-primary {
  background: var(--color-primary);
  color: #ffffff;
  box-shadow: 0 8rpx 24rpx rgba(63, 143, 114, 0.22);
}

@keyframes quick-add-enter {
  from { transform: translateY(32rpx); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

/* ============ 时长选择弹层 ============ */
.quick-duration-layer {
  position: fixed;
  inset: 0;
  z-index: 22;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  box-sizing: border-box;
  background: rgba(36, 76, 67, 0.22);
}

.quick-duration-panel {
  width: 100%;
  max-height: calc(100vh - 180rpx);
  overflow-y: auto;
  box-sizing: border-box;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  border-radius: 32rpx 32rpx 0 0;
  background: var(--color-card);
  box-shadow: var(--shadow-float);
  animation: quick-duration-enter 200ms ease-out both;
}

.quick-duration-header {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  height: 104rpx;
  padding: 0 28rpx;
  box-sizing: border-box;
  border-bottom: 1rpx solid var(--color-border-soft);
}

.quick-duration-cancel,
.quick-duration-spacer {
  width: 120rpx;
  min-width: 0;
}

.quick-duration-cancel {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  height: 72rpx;
  color: var(--color-text-secondary);
  font-size: 28rpx;
}

.quick-duration-title {
  flex: 1;
  min-width: 0;
  color: var(--color-text-main);
  font-size: 32rpx;
  font-weight: 700;
  text-align: center;
}

.quick-duration-options {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16rpx;
  width: 100%;
  box-sizing: border-box;
  padding: 28rpx 28rpx 8rpx;
}

.quick-duration-option {
  position: relative;
  min-width: 0;
  min-height: 86rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  border: 1rpx solid var(--color-border);
  border-radius: 22rpx;
  background: var(--color-card-soft);
  color: var(--color-text-main);
  font-weight: 650;
  box-shadow: var(--shadow-card-soft);
}

.quick-duration-option.is-selected {
  border-color: var(--color-primary);
  background: var(--color-primary-soft);
  color: var(--color-primary);
  box-shadow: inset 0 0 0 1rpx rgba(63, 143, 114, 0.1);
}

.quick-duration-option-label {
  font-size: 27rpx;
}

.quick-duration-check {
  position: absolute;
  top: 10rpx;
  right: 10rpx;
  width: 28rpx;
  height: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--color-primary);
  color: #ffffff;
  font-size: 20rpx;
}

@keyframes quick-duration-enter {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

/* ============ 日历弹层 ============ */
.calendar-layer {
  position: fixed;
  inset: 0;
  z-index: 18;
  box-sizing: border-box;
  padding: 330rpx 48rpx 0;
}

.calendar-mask {
  position: absolute;
  inset: 0;
  background: rgba(36, 76, 67, 0.16);
}

.calendar-panel {
  position: relative;
  width: 100%;
  max-width: 640rpx;
  box-sizing: border-box;
  margin: 0 auto;
  padding: 28rpx 32rpx 24rpx;
  border: 1rpx solid var(--color-border);
  border-radius: 28rpx;
  background: var(--color-card);
  box-shadow: var(--shadow-float);
}

.calendar-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
}

.calendar-title {
  color: var(--color-primary-deep);
  font-size: 30rpx;
  font-weight: 800;
  line-height: 40rpx;
}

.calendar-nav {
  width: 54rpx;
  height: 54rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  border-radius: 999rpx;
  color: var(--color-primary);
  font-size: 42rpx;
  line-height: 1;
}

.calendar-nav:active {
  background: var(--color-primary-soft);
}

.calendar-week-row,
.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
}

.calendar-week-row {
  margin-top: 22rpx;
}

.calendar-week-label {
  color: var(--color-text-muted);
  font-size: 22rpx;
  font-weight: 650;
  line-height: 34rpx;
  text-align: center;
}

.calendar-grid {
  row-gap: 12rpx;
  margin-top: 12rpx;
}

.calendar-day {
  position: relative;
  min-height: 58rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-text-main);
  font-size: 25rpx;
  font-weight: 700;
}

.calendar-day-number {
  width: 50rpx;
  height: 50rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
}

.calendar-day.is-outside {
  color: #C2CBC6;
}

.calendar-day.is-today:not(.is-selected) .calendar-day-number {
  background: var(--color-primary-soft);
  color: var(--color-primary);
}

.calendar-day.is-selected .calendar-day-number {
  background: var(--color-primary);
  color: #ffffff;
  box-shadow: 0 8rpx 18rpx rgba(63, 143, 114, 0.22);
}

.calendar-dot {
  position: absolute;
  bottom: 2rpx;
  left: 50%;
  width: 7rpx;
  height: 7rpx;
  border-radius: 50%;
  background: var(--color-warning);
  transform: translateX(-50%);
}

.calendar-dot.is-completed {
  background: var(--color-success);
}

.calendar-day.is-selected .calendar-dot {
  background: #ffffff;
}

.calendar-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  margin-top: 26rpx;
  padding-top: 22rpx;
  border-top: 1rpx solid var(--color-border-soft);
}

.calendar-today {
  min-height: 58rpx;
  display: flex;
  align-items: center;
  color: var(--color-primary);
  font-size: 26rpx;
  font-weight: 700;
}

.calendar-confirm {
  min-width: 112rpx;
  height: 58rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  background: var(--color-primary);
  color: #ffffff;
  font-size: 25rpx;
  font-weight: 700;
  box-shadow: 0 8rpx 18rpx rgba(63, 143, 114, 0.18);
}

/* ============ 响应式 ============ */
@media (max-width: 340px) {
  .today-page {
    padding-right: 22rpx;
    padding-left: 22rpx;
  }

  .today-topbar {
    padding-right: 150rpx;
  }

  .action-hero-count text:first-child {
    font-size: 48rpx;
  }

  .progress-ring {
    width: 96rpx;
    height: 96rpx;
  }

  .progress-ring-inner {
    width: 76rpx;
    height: 76rpx;
  }
}

/* ============ 行动完成庆祝面板 ============ */
.completion-layer {
  position: fixed;
  inset: 0;
  z-index: 30;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.completion-mask {
  position: absolute;
  inset: 0;
  background: rgba(35, 59, 52, 0.38);
  opacity: 0;
  transition: opacity 240ms ease;
  backdrop-filter: blur(4rpx);
}

.completion-panel {
  position: relative;
  width: 100%;
  max-width: 750rpx;
  max-height: calc(100vh - 96rpx);
  overflow-y: auto;
  box-sizing: border-box;
  padding: 18rpx 32rpx calc(34rpx + env(safe-area-inset-bottom));
  border-radius: 44rpx 44rpx 0 0;
  background: #FBFCF8;
  box-shadow: 0 -18rpx 56rpx rgba(31, 70, 58, 0.18);
  transform: translateY(100%);
  transition: transform 260ms cubic-bezier(0.22, 0.78, 0.24, 1);
}

.completion-layer.is-visible .completion-mask {
  opacity: 1;
}

.completion-layer.is-visible .completion-panel {
  transform: translateY(0);
}

.completion-grabber {
  width: 72rpx;
  height: 8rpx;
  margin: 0 auto 30rpx;
  border-radius: 999rpx;
  background: #DDE5E0;
}

.completion-celebration {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.completion-badge {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  background: #356859;
  color: #FFFFFF;
  font-size: 56rpx;
  box-shadow: 0 16rpx 30rpx rgba(53, 104, 89, 0.28);
}

.completion-badge .t-icon {
  font-size: 58rpx;
  font-weight: 800;
}

.completion-sparkle {
  position: absolute;
  top: -16rpx;
  right: -12rpx;
  color: #F8D26A;
  font-size: 40rpx;
  line-height: 1;
  text-shadow: 0 4rpx 10rpx rgba(200, 151, 25, 0.24);
}

.completion-title {
  margin-top: 28rpx;
  color: #233B34;
  font-size: 38rpx;
  line-height: 48rpx;
  font-weight: 700;
}

.completion-copy {
  margin-top: 8rpx;
  color: #71827A;
  font-size: 25rpx;
  line-height: 36rpx;
}

.completion-stats {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 20rpx;
  margin-top: 38rpx;
}

.completion-stat-card {
  display: flex;
  align-items: center;
  min-width: 0;
  min-height: 112rpx;
  box-sizing: border-box;
  padding: 20rpx;
  border: 1rpx solid #EEF2EF;
  border-radius: 24rpx;
  background: #FFFFFF;
  box-shadow: 0 8rpx 20rpx rgba(31, 70, 58, 0.07);
}

.completion-stat-icon {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  justify-content: center;
  width: 68rpx;
  height: 68rpx;
  margin-right: 18rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 700;
}

.completion-stat-icon.is-green {
  background: #EAF5EF;
  color: #356859;
}

.completion-stat-icon.is-yellow {
  background: #FFF2C9;
  color: #B68618;
}

.completion-stat-value {
  overflow: hidden;
  color: #233B34;
  font-size: 32rpx;
  line-height: 40rpx;
  font-weight: 800;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.completion-stat-label {
  margin-top: 2rpx;
  color: #71827A;
  font-size: 22rpx;
  line-height: 30rpx;
  white-space: nowrap;
}

.completion-actions {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
  margin-top: 38rpx;
}

.completion-share-button,
.completion-continue-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 88rpx;
  box-sizing: border-box;
  margin: 0;
  border-radius: 44rpx;
  font-size: 28rpx;
  font-weight: 700;
  line-height: 1;
}

.completion-share-button::after,
.completion-continue-button::after {
  border: 0;
}

.completion-share-button {
  background: #356859;
  color: #FFFFFF;
  box-shadow: 0 12rpx 26rpx rgba(53, 104, 89, 0.22);
}

.completion-continue-button {
  border: 1rpx solid #D7E4DE;
  background: #FFFFFF;
  color: #356859;
}

.completion-share-icon {
  margin-right: 12rpx;
  font-size: 34rpx;
  font-weight: 500;
}

.completion-arrow {
  margin-left: 12rpx;
  font-size: 34rpx;
  font-weight: 500;
}

.completion-share-button:active,
.completion-continue-button:active {
  transform: scale(0.985);
}

@media (max-width: 340px) {
  .completion-panel {
    padding-right: 24rpx;
    padding-left: 24rpx;
  }

  .completion-stat-card {
    padding: 16rpx;
  }

  .completion-stat-icon {
    width: 60rpx;
    height: 60rpx;
    margin-right: 12rpx;
  }
}

/* =========================================================
 * 墨绿成长：与薄荷绿明确分层的深色主题
 * ========================================================= */
.today-page[data-theme="inkGreen"] {
  background:
    radial-gradient(circle at 94% 18%, rgba(90, 142, 121, 0.24), transparent 32%),
    radial-gradient(circle at 0 94%, rgba(70, 122, 101, 0.22), transparent 30%),
    linear-gradient(180deg, #173F34 0%, #1D493C 58%, #15382F 100%);
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .today-greeting {
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .today-subtitle {
  color: rgba(234, 245, 239, 0.72);
}

.today-page[data-theme="inkGreen"] .week-card {
  border-color: rgba(255, 255, 255, 0.10);
  background: rgba(36, 84, 70, 0.92);
  box-shadow: 0 16rpx 36rpx rgba(8, 35, 27, 0.24);
}

.today-page[data-theme="inkGreen"] .week-title,
.today-page[data-theme="inkGreen"] .week-title text,
.today-page[data-theme="inkGreen"] .week-label,
.today-page[data-theme="inkGreen"] .week-number {
  color: rgba(255, 255, 255, 0.88);
}

.today-page[data-theme="inkGreen"] .week-switch {
  color: rgba(255, 255, 255, 0.74);
}

.today-page[data-theme="inkGreen"] .week-chevron {
  border-right-color: rgba(255, 255, 255, 0.74);
  border-bottom-color: rgba(255, 255, 255, 0.74);
}

.today-page[data-theme="inkGreen"] .week-day.is-selected .week-number {
  background: #F8D26A;
  color: #1F4E40;
  box-shadow: 0 8rpx 18rpx rgba(248, 210, 106, 0.20);
}

.today-page[data-theme="inkGreen"] .week-day.is-selected .week-label {
  color: #F8D26A;
}

.today-page[data-theme="inkGreen"] .week-day.is-selected::after {
  background: #F8D26A;
}

.today-page[data-theme="inkGreen"] .today-action-panel {
  border-color: rgba(255, 255, 255, 0.11);
  background: linear-gradient(145deg, #245A49 0%, #1F5142 54%, #1A4438 100%);
  box-shadow: 0 18rpx 42rpx rgba(7, 31, 24, 0.28);
}

.today-page[data-theme="inkGreen"] .today-action-hero {
  background:
    radial-gradient(circle at 84% 38%, rgba(248, 210, 106, 0.12), transparent 28%),
    linear-gradient(140deg, #2B6955 0%, #245A49 60%, #1D4A3D 100%);
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .action-hero-label {
  color: #BBD9CC;
}

.today-page[data-theme="inkGreen"] .action-hero-count,
.today-page[data-theme="inkGreen"] .action-unit,
.today-page[data-theme="inkGreen"] .action-hero-goal {
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .action-hero-goal {
  color: rgba(255, 255, 255, 0.72);
}

.today-page[data-theme="inkGreen"] .progress-ring {
  box-shadow: 0 10rpx 24rpx rgba(8, 35, 27, 0.28);
}

.today-page[data-theme="inkGreen"] .progress-ring-inner {
  background: #F8FBF9;
  color: #1F5B4A;
}

.today-page[data-theme="inkGreen"] .today-action-list {
  background: transparent;
}

.today-page[data-theme="inkGreen"] .today-task-row {
  border-color: rgba(255, 255, 255, 0.12);
  background: rgba(255, 255, 255, 0.07);
}

.today-page[data-theme="inkGreen"] .task-type-icon {
  background: rgba(234, 245, 239, 0.14);
  color: #D6EBE1;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-book::before,
.today-page[data-theme="inkGreen"] .task-type-icon.icon-note::before,
.today-page[data-theme="inkGreen"] .task-type-icon.icon-audio::before {
  border-color: #D6EBE1;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-book::after,
.today-page[data-theme="inkGreen"] .task-type-icon.icon-note::after {
  background: #D6EBE1;
  box-shadow: 0 6rpx 0 #D6EBE1;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-book::after {
  box-shadow: none;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-audio::before {
  border-bottom-color: transparent;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-audio::after {
  border-right-color: #D6EBE1;
  border-left-color: #D6EBE1;
}

.today-page[data-theme="inkGreen"] .task-title,
.today-page[data-theme="inkGreen"] .action-empty-inline {
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .task-desc,
.today-page[data-theme="inkGreen"] .action-empty-inline text {
  color: rgba(255, 255, 255, 0.64);
}

.today-page[data-theme="inkGreen"] .task-tail-check {
  border-color: rgba(255, 255, 255, 0.34);
}

.today-page[data-theme="inkGreen"] .task-tail-check.is-done {
  border-color: #F8D26A;
  background: #F8D26A;
  color: #1F5B4A;
}

.today-page[data-theme="inkGreen"] .inline-add-action {
  border-color: rgba(255, 255, 255, 0.28);
  background: rgba(255, 255, 255, 0.08);
  color: #EAF5EF;
}

.today-page[data-theme="inkGreen"] .today-data-card {
  border-color: rgba(255, 255, 255, 0.10);
  background: rgba(35, 81, 67, 0.94);
  box-shadow: 0 16rpx 36rpx rgba(8, 35, 27, 0.22);
}

.today-page[data-theme="inkGreen"] .today-data-title,
.today-page[data-theme="inkGreen"] .today-data-value {
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .today-data-label,
.today-page[data-theme="inkGreen"] .today-data-value text {
  color: rgba(255, 255, 255, 0.66);
}

.today-page[data-theme="inkGreen"] .today-data-item + .today-data-item {
  border-left-color: rgba(255, 255, 255, 0.12);
}

.today-page[data-theme="inkGreen"] .daily-nudge-card {
  background: linear-gradient(135deg, #F8D26A, #F3C85A);
  box-shadow: 0 12rpx 28rpx rgba(8, 35, 27, 0.16);
}

.today-page[data-theme="inkGreen"] .quote-mark,
.today-page[data-theme="inkGreen"] .daily-nudge-star,
.today-page[data-theme="inkGreen"] .daily-nudge-text {
  color: #5E4A16;
}

/* 墨绿主题：当前页面真实使用的是 today-action-* 结构。 */
.today-page[data-theme="inkGreen"] {
  --today-primary: #356859;
  --today-primary-deep: #1F5B4A;
  --today-accent: #7FAA91;
  background: #F5F7F2;
}

.today-page[data-theme="inkGreen"] .today-action-hero {
  background:
    radial-gradient(circle at 82% 36%, rgba(255, 255, 255, 0.14), transparent 32%),
    linear-gradient(135deg, #1F5B4A 0%, #356859 62%, #4C806E 100%);
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .action-hero-label,
.today-page[data-theme="inkGreen"] .action-hero-count,
.today-page[data-theme="inkGreen"] .action-unit,
.today-page[data-theme="inkGreen"] .action-hero-goal {
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner {
  background: rgba(255, 255, 255, 0.94);
  color: #1F5B4A;
}

.today-page[data-theme="inkGreen"] .week-card,
.today-page[data-theme="inkGreen"] .today-action-list,
.today-page[data-theme="inkGreen"] .today-data-card {
  background: #FFFFFF;
  border-color: rgba(31, 91, 74, 0.10);
}

.today-page[data-theme="inkGreen"] .today-bottom-action,
.today-page[data-theme="inkGreen"] .quick-add-primary,
.today-page[data-theme="inkGreen"] .calendar-confirm {
  background: #356859;
  box-shadow: 0 10rpx 24rpx rgba(53, 104, 89, 0.22);
}

/* =========================================================
 * V2 设计稿最终对齐：今日页
 * ========================================================= */
.today-page,
.today-page[data-theme="inkGreen"] {
  padding: 26rpx 28rpx calc(164rpx + env(safe-area-inset-bottom));
  background:
    radial-gradient(circle at 12% 5%, rgba(221, 235, 221, 0.72), transparent 30%),
    radial-gradient(circle at 92% 19%, rgba(244, 201, 93, 0.13), transparent 28%),
    linear-gradient(180deg, #FCFCF8 0%, #F7F8F3 46%, #F5F7F2 100%);
  color: #1F2D28;
}

.today-topbar {
  min-height: 100rpx;
  margin-bottom: 6rpx;
  padding: 22rpx 170rpx 8rpx 8rpx;
}

.today-greeting {
  color: #1F2D28;
  font-size: 36rpx;
  line-height: 46rpx;
  font-weight: 700;
}

.today-subtitle {
  margin-top: 4rpx;
  color: #71827A;
  font-size: 24rpx;
  line-height: 34rpx;
  font-weight: 500;
}

.week-card {
  height: 190rpx;
  min-height: 190rpx;
  margin-top: 12rpx;
  padding: 22rpx 26rpx 18rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.92);
  border-radius: 28rpx;
  background: rgba(255, 255, 252, 0.82);
  box-shadow: 0 14rpx 34rpx rgba(47, 107, 87, 0.075);
}

.week-head {
  margin-bottom: 12rpx;
}

.week-title {
  color: #1F2D28;
  font-size: 30rpx;
  font-weight: 700;
}

.week-switch,
.today-page[data-theme="inkGreen"] .week-switch {
  width: auto;
  height: 44rpx;
  gap: 10rpx;
  padding: 0 6rpx 0 14rpx;
  border-radius: 22rpx;
  background: transparent;
  color: #71827A;
  font-size: 22rpx;
  font-weight: 600;
  box-shadow: none;
}

.week-chevron {
  width: 10rpx;
  height: 10rpx;
  border-right-color: #71827A;
  border-bottom-color: #71827A;
  border-width: 2rpx;
}

.week-day {
  gap: 6rpx;
}

.week-label,
.week-number {
  color: #4F655E;
}

.week-day.is-selected .week-number,
.today-page[data-theme="inkGreen"] .week-day.is-selected .week-number {
  background: #F4C95D;
  color: #2F4A3F;
  box-shadow: 0 7rpx 16rpx rgba(244, 201, 93, 0.26);
}

.week-day.is-selected::after {
  width: 7rpx;
  height: 7rpx;
  margin-top: -1rpx;
  border-radius: 50%;
  background: #D5A82D;
  content: "";
}

.week-day.is-selected .week-label,
.today-page[data-theme="inkGreen"] .week-day.is-selected .week-label {
  color: #B78919;
}

.today-action-panel {
  margin-top: 28rpx;
  border: 1rpx solid rgba(229, 234, 227, 0.94);
  border-radius: 28rpx;
  background: rgba(252, 252, 248, 0.94);
  box-shadow: 0 16rpx 38rpx rgba(31, 45, 40, 0.07);
}

.today-action-hero,
.today-page[data-theme="inkGreen"] .today-action-hero {
  min-height: 172rpx;
  padding: 28rpx 30rpx 22rpx;
  background:
    radial-gradient(circle at 86% 44%, rgba(255, 255, 255, 0.76), transparent 32%),
    linear-gradient(140deg, #E4F0E6 0%, #EAF3EA 64%, #F7FAF5 100%);
  color: #1F2D28;
}

.today-page[data-theme="inkGreen"] .action-hero-label {
  color: #356859;
}

.today-page[data-theme="inkGreen"] .action-hero-count {
  color: #1F2D28;
}

.today-page[data-theme="inkGreen"] .action-unit,
.today-page[data-theme="inkGreen"] .action-hero-goal {
  color: #71827A;
}

.action-hero-count text:first-child {
  font-size: 52rpx;
}

.progress-ring {
  width: 112rpx;
  height: 112rpx;
  padding: 8rpx;
  box-sizing: border-box;
  box-shadow: 0 10rpx 24rpx rgba(47, 107, 87, 0.12);
}

.progress-ring-inner,
.today-page[data-theme="inkGreen"] .progress-ring-inner {
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.96);
  color: #1F5B4A;
}

.today-action-list,
.today-page[data-theme="inkGreen"] .today-action-list {
  padding: 10rpx 16rpx 18rpx;
  background: rgba(252, 252, 248, 0.96);
}

.today-task-row {
  min-height: 80rpx;
  gap: 16rpx;
  padding: 2rpx 18rpx;
  border: 1rpx solid #E1EAE5;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.82);
}

.today-task-row + .today-task-row {
  margin-top: 8rpx;
}

.task-type-icon {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: #EAF5EF;
  color: #356859;
}

.task-type-icon::before,
.task-type-icon::after,
.task-type-icon view {
  position: absolute;
  box-sizing: border-box;
  content: "";
}

.task-type-icon.icon-book::before {
  width: 22rpx;
  height: 22rpx;
  border: 3rpx solid #356859;
  border-radius: 4rpx 2rpx 2rpx 4rpx;
}

.task-type-icon.icon-book::after {
  width: 2rpx;
  height: 19rpx;
  background: #356859;
}

.task-type-icon.icon-note::before {
  width: 20rpx;
  height: 24rpx;
  border: 3rpx solid #356859;
  border-radius: 3rpx;
}

.task-type-icon.icon-note::after {
  width: 10rpx;
  height: 2rpx;
  margin-top: -6rpx;
  background: #356859;
  box-shadow: 0 6rpx 0 #356859;
}

.task-type-icon.icon-audio::before {
  width: 22rpx;
  height: 22rpx;
  border: 3rpx solid #356859;
  border-bottom-color: transparent;
  border-radius: 50% 50% 0 0;
}

.task-type-icon.icon-audio::after {
  width: 25rpx;
  height: 13rpx;
  border-right: 5rpx solid #356859;
  border-left: 5rpx solid #356859;
  border-radius: 4rpx;
}

.task-title {
  color: #1F2D28;
  font-size: 27rpx;
  font-weight: 600;
}

.task-desc {
  color: #71827A;
}

.task-tail-check {
  width: 38rpx;
  height: 38rpx;
  border: 2rpx solid #C9D8D1;
  border-radius: 50%;
  color: #FFFFFF;
}

.task-tail-check.is-done {
  border-color: #356859;
  background: #356859;
}

.inline-add-action {
  height: 72rpx;
  margin-top: 12rpx;
  border: 1rpx solid #CFE2D2;
  border-radius: 17rpx;
  background: #F3FAF3;
  color: #356859;
  font-weight: 700;
}

.today-data-card,
.today-page[data-theme="inkGreen"] .today-data-card {
  margin-top: 28rpx;
  padding: 26rpx 26rpx;
  border: 1rpx solid rgba(229, 234, 227, 0.94);
  border-radius: 24rpx;
  background: rgba(255, 255, 252, 0.86);
  box-shadow: 0 12rpx 30rpx rgba(31, 45, 40, 0.055);
  transition: transform 140ms ease, box-shadow 140ms ease;
}

.today-data-card--pressed {
  transform: scale(0.988);
  box-shadow: 0 8rpx 20rpx rgba(31, 45, 40, 0.045);
}

.today-data-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.today-data-entry {
  display: flex;
  align-items: center;
  color: #356859;
  font-size: 22rpx;
  font-weight: 600;
}

.today-data-entry text {
  margin-left: 6rpx;
  font-size: 30rpx;
  line-height: 1;
}

.today-data-grid {
  margin-top: 18rpx;
}

.today-data-item {
  padding: 0 14rpx;
}

.today-data-value {
  color: #1F2D28;
  font-size: 36rpx;
  font-weight: 700;
}

.daily-nudge-card {
  position: relative;
  min-height: 76rpx;
  margin-top: 24rpx;
  padding: 18rpx 56rpx 18rpx 62rpx;
  border: 0;
  border: 1rpx solid #F5DFA5;
  border-radius: 20rpx;
  background: #FFF1C7;
  box-shadow: 0 10rpx 22rpx rgba(244, 201, 93, 0.10);
}

/* =========================================================
 * V4 浅色奶油 · 今日页轻插画氛围最终覆盖
 * 顶部插画背景 + 奶油底 + 浅绿今日行动卡 + 金色点缀
 * 不改业务结构，仅做视觉统一升级
 * ========================================================= */

/* --- CSS 变量统一为奶油浅色系 --- */
.today-page[data-theme="inkGreen"] {
  --today-primary: #2F765E;
  --today-primary-deep: #294F43;
  --today-accent: #5EB28B;
  --today-text: #20322D;
  --today-secondary: #7D8C86;
  --today-muted: #93A099;
  --today-border: #E7ECE5;
  --today-gold: #F2C34B;
  background: #F6F3EA;
  color: #20322D;
}

/* --- 顶部背景插画（轻插画氛围，无文字） --- */
.today-header {
  position: relative;
  overflow: hidden;
  margin: -28rpx -32rpx 0;
  height: 300rpx;
}

.today-header-bg {
  position: absolute;
  left: 0;
  top: -12%;
  width: 100%;
  height: 124%;
  z-index: 0;
  pointer-events: none;
}

/* 底部柔和过渡：让插画波浪底部融入页面奶油白背景 */
.today-header::after {
  content: "";
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  height: 70rpx;
  background: linear-gradient(to bottom, rgba(246, 243, 234, 0) 0%, rgba(246, 243, 234, 0.5) 55%, #F6F3EA 100%);
  z-index: 1;
  pointer-events: none;
}

.today-header-content {
  position: relative;
  z-index: 2;
  padding: 52rpx 48rpx 0;
  box-sizing: border-box;
}

/* --- 问候文案（由 WXML 渲染，不写进背景图） --- */
.today-page[data-theme="inkGreen"] .today-header .today-greeting {
  color: #20322D;
  font-size: 54rpx;
  font-weight: 900;
  line-height: 1.12;
  letter-spacing: -1rpx;
}

.today-page[data-theme="inkGreen"] .today-header .today-subtitle {
  margin-top: 10rpx;
  color: #7D8C86;
  font-size: 27rpx;
  font-weight: 600;
  line-height: 1.4;
}

/* --- 日历卡上移叠加在背景下沿，承接顶部氛围 --- */
.today-page[data-theme="inkGreen"] .week-card {
  margin-top: -130rpx;
  position: relative;
  z-index: 3;
  background: #FFFDF7;
  border: 1rpx solid rgba(255, 255, 255, 0.76);
  border-radius: 32rpx;
  box-shadow: 0 20rpx 52rpx rgba(48, 71, 60, 0.10);
}

/* --- 卡片统一奶油白 --- */
.today-page[data-theme="inkGreen"] .card,
.today-page[data-theme="inkGreen"] .today-action-panel,
.today-page[data-theme="inkGreen"] .today-data-card,
.today-page[data-theme="inkGreen"] .daily-nudge-card {
  background: #FFFDF7;
  border: 1rpx solid rgba(231, 236, 229, 0.88);
  border-radius: 32rpx;
  box-shadow: 0 20rpx 52rpx rgba(48, 71, 60, 0.08);
}

/* --- 今日行动卡：顶部浅绿渐变 + 圆角 --- */
.today-page[data-theme="inkGreen"] .today-action-panel {
  margin-top: 28rpx;
  overflow: hidden;
  border-radius: 32rpx;
}

.today-page[data-theme="inkGreen"] .today-action-hero {
  min-height: 220rpx;
  padding: 32rpx 32rpx 28rpx;
  background: linear-gradient(135deg, #F1F8F2 0%, #F8FBF6 100%);
  border-radius: 32rpx 32rpx 0 0;
}

/* --- 今日行动标签：浅绿胶囊 --- */
.today-page[data-theme="inkGreen"] .action-hero-label {
  display: inline-flex;
  align-items: center;
  height: 48rpx;
  padding: 0 20rpx;
  border-radius: 999rpx;
  background: #EEF5EF;
  color: #2F765E;
  font-size: 24rpx;
  font-weight: 800;
}

/* --- 行动数量大数字 --- */
.today-page[data-theme="inkGreen"] .action-hero-count text:first-child {
  color: #20322D;
  font-size: 64rpx;
  font-weight: 900;
  line-height: 1;
}

.today-page[data-theme="inkGreen"] .action-hero-count .action-total,
.today-page[data-theme="inkGreen"] .action-hero-count .action-unit {
  color: #7D8C86;
  font-size: 26rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .action-hero-goal {
  margin-top: 10rpx;
  color: #3A5C51;
  font-size: 28rpx;
  font-weight: 800;
}

/* --- 进度环：放大 + 主绿 --- */
.today-page[data-theme="inkGreen"] .progress-ring {
  width: 160rpx;
  height: 160rpx;
  padding: 0;
  box-sizing: border-box;
  box-shadow: 0 16rpx 34rpx rgba(47, 118, 94, 0.10);
}

.today-page[data-theme="inkGreen"] .progress-ring-inner {
  width: 100%;
  height: 100%;
  background: #FFFFFF;
  border-radius: 50%;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner view {
  color: #2F765E;
  font-size: 40rpx;
  font-weight: 900;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner text {
  color: #668077;
  font-size: 22rpx;
  font-weight: 700;
}

/* --- 任务列表：白底圆角 --- */
.today-page[data-theme="inkGreen"] .today-action-list {
  padding: 24rpx;
  background: #FFFFFF;
  border-radius: 0 0 32rpx 32rpx;
}

/* --- 任务行：卡片化 --- */
.today-page[data-theme="inkGreen"] .today-task-row {
  min-height: 120rpx;
  gap: 20rpx;
  padding: 22rpx 24rpx;
  border: 1rpx solid #EEF1ED;
  border-radius: 24rpx;
  background: #FFFFFF;
  box-shadow: none;
}

.today-page[data-theme="inkGreen"] .today-task-row + .today-task-row {
  margin-top: 16rpx;
}

/* --- 任务图标：圆形浅绿 --- */
.today-page[data-theme="inkGreen"] .task-type-icon {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #EEF6F0;
  color: #2F765E;
}

/* --- 完成勾选：圆形 --- */
.today-page[data-theme="inkGreen"] .task-tail-check {
  width: 56rpx;
  height: 56rpx;
  border: 4rpx solid #D7E2DA;
  border-radius: 50%;
}

.today-page[data-theme="inkGreen"] .task-tail-check.is-done {
  background: #2F765E;
  border-color: #2F765E;
}

/* --- 展开收起链接 --- */
.today-page[data-theme="inkGreen"] .action-list-toggle {
  color: #7D8C86;
  font-size: 26rpx;
  font-weight: 700;
}

/* --- 添加今日行动按钮：浅绿底 --- */
.today-page[data-theme="inkGreen"] .inline-add-action {
  height: 88rpx;
  margin-top: 12rpx;
  border-radius: 24rpx;
  background: #F2F8F2;
  border: 1rpx solid #E4EEE5;
  color: #2F765E;
  font-size: 30rpx;
  font-weight: 800;
}

/* --- 今日数据卡 --- */
.today-page[data-theme="inkGreen"] .today-data-value {
  color: #2F765E;
  font-size: 40rpx;
  font-weight: 800;
}

/* --- 每日提示卡：浅金 --- */
.today-page[data-theme="inkGreen"] .daily-nudge-card {
  border: 1rpx solid #F5DFA5;
  background: #FFF7DC;
  border-radius: 24rpx;
}

/* --- 文字颜色统一 --- */
.today-page[data-theme="inkGreen"] .week-title,
.today-page[data-theme="inkGreen"] .task-title,
.today-page[data-theme="inkGreen"] .today-data-title,
.today-page[data-theme="inkGreen"] .state-title,
.today-page[data-theme="inkGreen"] .empty-title {
  color: #20322D;
}

.today-page[data-theme="inkGreen"] .today-subtitle,
.today-page[data-theme="inkGreen"] .action-hero-count,
.today-page[data-theme="inkGreen"] .task-desc,
.today-page[data-theme="inkGreen"] .today-data-label,
.today-page[data-theme="inkGreen"] .week-switch,
.today-page[data-theme="inkGreen"] .today-data-entry {
  color: #7D8C86;
}

/* --- 周日历今日：金色圆点 --- */
.today-page[data-theme="inkGreen"] .week-day.is-today .week-number {
  background: #F2C34B;
  color: #24352E;
  box-shadow: 0 12rpx 24rpx rgba(242, 195, 75, 0.22);
}

.quote-mark {
  position: absolute;
  left: 24rpx;
  top: 16rpx;
  color: #C9A95A;
  font-size: 44rpx;
}

.daily-nudge-text {
  color: #4D3B1F;
  font-size: 23rpx;
  line-height: 34rpx;
  font-weight: 500;
}

.daily-nudge-star {
  position: absolute;
  right: 22rpx;
  top: 50%;
  color: #D2B267;
  font-size: 28rpx;
  transform: translateY(-50%);
}

/* ============ V2 设计稿级联锁定：覆盖早期 inkGreen 高特异性规则 ============ */
.today-page[data-theme="inkGreen"] {
  padding: 32rpx 32rpx calc(164rpx + env(safe-area-inset-bottom));
  background:
    radial-gradient(circle at 14% 4%, rgba(221, 235, 221, 0.82), transparent 32%),
    radial-gradient(circle at 92% 18%, rgba(244, 201, 93, 0.14), transparent 30%),
    linear-gradient(180deg, #FCFCF8 0%, #F7F8F3 46%, #F5F7F2 100%);
  color: #1F2D28;
}

.today-page[data-theme="inkGreen"] .today-topbar {
  min-height: 184rpx;
  margin-bottom: 0;
  padding: 80rpx 170rpx 8rpx 8rpx;
}

.today-page[data-theme="inkGreen"] .today-greeting {
  color: #1F2D28;
  font-size: 50rpx;
  line-height: 62rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .today-subtitle {
  margin-top: 6rpx;
  color: #71827A;
  font-size: 28rpx;
  line-height: 40rpx;
  font-weight: 500;
  white-space: nowrap;
}

.today-page[data-theme="inkGreen"] .week-card {
  height: auto;
  min-height: 264rpx;
  margin-top: 32rpx;
  padding: 30rpx 32rpx 28rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.92);
  border-radius: 44rpx;
  background: rgba(255, 255, 252, 0.86);
  box-shadow: 0 14rpx 34rpx rgba(47, 107, 87, 0.08);
}

.today-page[data-theme="inkGreen"] .week-head {
  margin-bottom: 22rpx;
}

.today-page[data-theme="inkGreen"] .week-title {
  color: #1F2D28;
  font-size: 40rpx;
  line-height: 52rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .week-title text {
  margin-left: 12rpx;
  color: #71827A;
  font-size: 28rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .week-switch {
  height: 52rpx;
  color: #71827A;
  font-size: 24rpx;
  font-weight: 600;
}

.today-page[data-theme="inkGreen"] .week-chevron {
  border-right-color: #71827A;
  border-bottom-color: #71827A;
}

.today-page[data-theme="inkGreen"] .week-day {
  gap: 12rpx;
}

.today-page[data-theme="inkGreen"] .week-label {
  height: 32rpx;
  color: #71827A;
  font-size: 28rpx;
  line-height: 32rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .week-number {
  width: 64rpx;
  height: 64rpx;
  color: #71827A;
  font-size: 28rpx;
  line-height: 64rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .week-day.is-selected .week-number {
  background: #F4C95D;
  color: #2F4A3F;
  font-size: 30rpx;
  font-weight: 800;
  box-shadow: 0 8rpx 18rpx rgba(244, 201, 93, 0.28);
}

.today-page[data-theme="inkGreen"] .week-day.is-selected .week-label {
  color: #C99A24;
}

/* 今日但未被选中：用浅绿柔和标记，避免与黄色选中态重复 */
.today-page[data-theme="inkGreen"] .week-day.is-today:not(.is-selected) .week-number {
  background: #EAF5EF;
  color: #356859;
  box-shadow: none;
}

.today-page[data-theme="inkGreen"] .week-day.is-today:not(.is-selected) .week-label {
  color: #356859;
}

.today-page[data-theme="inkGreen"] .week-day.is-selected::after {
  background: #D5A82D;
}

.today-page[data-theme="inkGreen"] .today-action-panel {
  margin-top: 32rpx;
  overflow: hidden;
  border: 1rpx solid #E5EAE3;
  border-radius: 44rpx;
  background: #FCFCF8;
  box-shadow: 0 16rpx 38rpx rgba(31, 45, 40, 0.07);
}

.today-page[data-theme="inkGreen"] .today-action-hero {
  min-height: 244rpx;
  padding: 32rpx 40rpx 30rpx;
  background:
    radial-gradient(circle at 86% 42%, rgba(255, 255, 255, 0.70), transparent 34%),
    linear-gradient(140deg, #E4F0E6 0%, #EAF3EA 66%, #F7FAF5 100%);
  color: #1F2D28;
}

.today-page[data-theme="inkGreen"] .action-hero-label {
  display: inline-flex;
  align-items: center;
  min-height: 48rpx;
  padding: 0 20rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.76);
  color: #356859;
  font-size: 24rpx;
  line-height: 48rpx;
  font-weight: 700;
  letter-spacing: 0;
}

.today-page[data-theme="inkGreen"] .action-hero-count {
  margin-top: 14rpx;
  color: #1F2D28;
}

.today-page[data-theme="inkGreen"] .action-hero-count text:first-child {
  font-size: 66rpx;
  line-height: 72rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .action-total {
  color: #71827A;
  font-size: 40rpx;
}

.today-page[data-theme="inkGreen"] .action-unit,
.today-page[data-theme="inkGreen"] .action-hero-goal {
  color: #71827A;
  font-size: 26rpx;
  font-weight: 600;
}

.today-page[data-theme="inkGreen"] .progress-ring {
  width: 144rpx;
  height: 144rpx;
  padding: 10rpx;
  box-shadow: 0 10rpx 24rpx rgba(47, 107, 87, 0.13);
}

.today-page[data-theme="inkGreen"] .progress-ring-inner {
  background: rgba(255, 255, 255, 0.96);
  color: #2F6B57;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner view {
  font-size: 38rpx;
  line-height: 44rpx;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner text {
  color: #71827A;
  font-size: 20rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .today-action-list {
  padding: 36rpx 32rpx 40rpx;
  background: #FCFCF8;
}

.today-page[data-theme="inkGreen"] .action-empty-inline {
  padding: 0 0 34rpx;
  color: #1F2D28;
  font-size: 42rpx;
  line-height: 54rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .action-empty-inline text {
  margin-top: 8rpx;
  color: #71827A;
  font-size: 28rpx;
  line-height: 40rpx;
  font-weight: 500;
}

.today-page[data-theme="inkGreen"] .today-task-row {
  min-height: 104rpx;
  gap: 18rpx;
  padding: 8rpx 20rpx;
  border: 1rpx solid #E1EAE5;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.84);
}

.today-page[data-theme="inkGreen"] .today-task-row + .today-task-row {
  margin-top: 14rpx;
}

.today-page[data-theme="inkGreen"] .task-type-icon {
  width: 48rpx;
  height: 48rpx;
  background: #EAF5EF;
  color: #356859;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-book::before,
.today-page[data-theme="inkGreen"] .task-type-icon.icon-note::before,
.today-page[data-theme="inkGreen"] .task-type-icon.icon-audio::before {
  border-color: #356859;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-audio::before {
  border-bottom-color: transparent;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-book::after,
.today-page[data-theme="inkGreen"] .task-type-icon.icon-note::after {
  background: #356859;
  box-shadow: 0 6rpx 0 #356859;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-book::after {
  box-shadow: none;
}

.today-page[data-theme="inkGreen"] .task-type-icon.icon-audio::after {
  border-right-color: #356859;
  border-left-color: #356859;
}

.today-page[data-theme="inkGreen"] .task-title {
  color: #1F2D28;
  font-size: 28rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .task-desc {
  color: #71827A;
  font-size: 23rpx;
}

.today-page[data-theme="inkGreen"] .task-tail-check {
  width: 44rpx;
  height: 44rpx;
  border-color: #C9D8D1;
  background: transparent;
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .task-tail-check.is-done {
  border-color: #356859;
  background: #356859;
  color: #FFFFFF;
}

.today-page[data-theme="inkGreen"] .inline-add-action {
  height: 96rpx;
  margin-top: 0;
  border: 1rpx solid #CFE2D2;
  border-radius: 28rpx;
  background: #F3FAF3;
  color: #356859;
  font-size: 30rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .today-data-card {
  margin-top: 48rpx;
  padding: 32rpx;
  border: 1rpx solid #E5EAE3;
  border-radius: 36rpx;
  background: rgba(255, 255, 252, 0.88);
  box-shadow: 0 12rpx 28rpx rgba(31, 45, 40, 0.055);
}

.today-page[data-theme="inkGreen"] .today-data-title {
  color: #1F2D28;
  font-size: 40rpx;
  line-height: 52rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .today-data-grid {
  margin-top: 26rpx;
}

.today-page[data-theme="inkGreen"] .today-data-value {
  color: #1F2D28;
  font-size: 48rpx;
  font-weight: 800;
}

.today-page[data-theme="inkGreen"] .today-data-value text {
  color: #71827A;
  font-size: 22rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .today-data-label {
  color: #71827A;
  font-size: 26rpx;
  font-weight: 600;
}

.today-page[data-theme="inkGreen"] .today-data-item + .today-data-item {
  border-left-color: #E5EAE3;
}

.today-page[data-theme="inkGreen"] .daily-nudge-card {
  min-height: 96rpx;
  margin-top: 32rpx;
  padding: 20rpx 58rpx 20rpx 64rpx;
  border: 1rpx solid #F5DFA5;
  border-radius: 32rpx;
  background: #FFF1C7;
  box-shadow: 0 10rpx 22rpx rgba(244, 201, 93, 0.12);
}

.today-page[data-theme="inkGreen"] .quote-mark {
  color: #A37D1B;
}

.today-page[data-theme="inkGreen"] .daily-nudge-text {
  color: #4D3B1F;
  font-size: 26rpx;
  line-height: 38rpx;
  font-weight: 600;
}

.today-page[data-theme="inkGreen"] .daily-nudge-star {
  color: #A37D1B;
}

@media (max-width: 340px) {
  .today-page,
  .today-page[data-theme="inkGreen"] {
    padding-right: 22rpx;
    padding-left: 22rpx;
  }

  .today-topbar,
  .today-page[data-theme="inkGreen"] .today-topbar {
    padding-right: 126rpx;
  }
}

/* =========================================================
 * V4 浅色奶油 · 今日页轻插画氛围最终覆盖（覆盖 V2）
 * 顶部插画背景 + 奶油底 + 浅绿今日行动卡 + 金色点缀
 * 不改业务结构，仅做视觉统一升级
 * ========================================================= */

/* --- CSS 变量统一为奶油浅色系 --- */
.today-page[data-theme="inkGreen"] {
  --today-primary: #2F765E;
  --today-primary-deep: #294F43;
  --today-accent: #5EB28B;
  --today-text: #20322D;
  --today-secondary: #7D8C86;
  --today-muted: #93A099;
  --today-border: #E7ECE5;
  --today-gold: #F2C34B;
  background: #F6F3EA;
  color: #20322D;
}

/* --- 顶部背景插画（轻插画氛围，无文字） --- */
.today-header {
  position: relative;
  overflow: hidden;
  margin: -28rpx -32rpx 0;
  height: 300rpx;
}

.today-header-bg {
  position: absolute;
  left: 0;
  top: -12%;
  width: 100%;
  height: 124%;
  z-index: 0;
  pointer-events: none;
}

/* 底部柔和过渡：让插画波浪底部融入页面奶油白背景 */
.today-header::after {
  content: "";
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  height: 70rpx;
  background: linear-gradient(to bottom, rgba(246, 243, 234, 0) 0%, rgba(246, 243, 234, 0.5) 55%, #F6F3EA 100%);
  z-index: 1;
  pointer-events: none;
}

.today-header-content {
  position: relative;
  z-index: 2;
  padding: 52rpx 48rpx 0;
  box-sizing: border-box;
}

/* --- 问候文案（由 WXML 渲染，不写进背景图） --- */
.today-page[data-theme="inkGreen"] .today-header .today-greeting {
  color: #20322D;
  font-size: 54rpx;
  font-weight: 900;
  line-height: 1.12;
  letter-spacing: -1rpx;
}

.today-page[data-theme="inkGreen"] .today-header .today-subtitle {
  margin-top: 10rpx;
  color: #7D8C86;
  font-size: 27rpx;
  font-weight: 600;
  line-height: 1.4;
}

/* --- 日历卡上移叠加在背景下沿，承接顶部氛围 --- */
.today-page[data-theme="inkGreen"] .week-card {
  margin-top: -130rpx;
  position: relative;
  z-index: 3;
  background: #FFFDF7;
  border: 1rpx solid rgba(255, 255, 255, 0.76);
  border-radius: 32rpx;
  box-shadow: 0 20rpx 52rpx rgba(48, 71, 60, 0.10);
}

/* --- 卡片统一奶油白 --- */
.today-page[data-theme="inkGreen"] .card,
.today-page[data-theme="inkGreen"] .today-action-panel,
.today-page[data-theme="inkGreen"] .today-data-card,
.today-page[data-theme="inkGreen"] .daily-nudge-card {
  background: #FFFDF7;
  border: 1rpx solid rgba(231, 236, 229, 0.88);
  border-radius: 32rpx;
  box-shadow: 0 20rpx 52rpx rgba(48, 71, 60, 0.08);
}

/* --- 今日行动卡：顶部浅绿渐变 + 圆角 --- */
.today-page[data-theme="inkGreen"] .today-action-panel {
  margin-top: 28rpx;
  overflow: hidden;
  border-radius: 32rpx;
}

.today-page[data-theme="inkGreen"] .today-action-hero {
  min-height: 220rpx;
  padding: 32rpx 32rpx 28rpx;
  background: linear-gradient(135deg, #F1F8F2 0%, #F8FBF6 100%);
  border-radius: 32rpx 32rpx 0 0;
}

/* --- 今日行动标签：浅绿胶囊 --- */
.today-page[data-theme="inkGreen"] .action-hero-label {
  display: inline-flex;
  align-items: center;
  height: 48rpx;
  padding: 0 20rpx;
  border-radius: 999rpx;
  background: #EEF5EF;
  color: #2F765E;
  font-size: 24rpx;
  font-weight: 800;
}

/* --- 行动数量大数字 --- */
.today-page[data-theme="inkGreen"] .action-hero-count text:first-child {
  color: #20322D;
  font-size: 64rpx;
  font-weight: 900;
  line-height: 1;
}

.today-page[data-theme="inkGreen"] .action-hero-count .action-total,
.today-page[data-theme="inkGreen"] .action-hero-count .action-unit {
  color: #7D8C86;
  font-size: 26rpx;
  font-weight: 700;
}

.today-page[data-theme="inkGreen"] .action-hero-goal {
  margin-top: 10rpx;
  color: #3A5C51;
  font-size: 28rpx;
  font-weight: 800;
}

/* --- 进度环：放大 + 主绿 --- */
.today-page[data-theme="inkGreen"] .progress-ring {
  width: 160rpx;
  height: 160rpx;
  padding: 0;
  box-sizing: border-box;
  box-shadow: 0 16rpx 34rpx rgba(47, 118, 94, 0.10);
}

.today-page[data-theme="inkGreen"] .progress-ring-inner {
  width: 100%;
  height: 100%;
  background: #FFFFFF;
  border-radius: 50%;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner view {
  color: #2F765E;
  font-size: 40rpx;
  font-weight: 900;
}

.today-page[data-theme="inkGreen"] .progress-ring-inner text {
  color: #668077;
  font-size: 22rpx;
  font-weight: 700;
}

/* --- 任务列表：白底圆角 --- */
.today-page[data-theme="inkGreen"] .today-action-list {
  padding: 24rpx;
  background: #FFFFFF;
  border-radius: 0 0 32rpx 32rpx;
}

/* --- 任务行：卡片化 --- */
.today-page[data-theme="inkGreen"] .today-task-row {
  min-height: 120rpx;
  gap: 20rpx;
  padding: 22rpx 24rpx;
  border: 1rpx solid #EEF1ED;
  border-radius: 24rpx;
  background: #FFFFFF;
  box-shadow: none;
}

.today-page[data-theme="inkGreen"] .today-task-row + .today-task-row {
  margin-top: 16rpx;
}

/* --- 任务图标：圆形浅绿 --- */
.today-page[data-theme="inkGreen"] .task-type-icon {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #EEF6F0;
  color: #2F765E;
}

/* --- 完成勾选：圆形 --- */
.today-page[data-theme="inkGreen"] .task-tail-check {
  width: 56rpx;
  height: 56rpx;
  border: 4rpx solid #D7E2DA;
  border-radius: 50%;
}

.today-page[data-theme="inkGreen"] .task-tail-check.is-done {
  background: #2F765E;
  border-color: #2F765E;
}

/* --- 展开收起链接 --- */
.today-page[data-theme="inkGreen"] .action-list-toggle {
  color: #7D8C86;
  font-size: 26rpx;
  font-weight: 700;
}

/* --- 添加今日行动按钮：浅绿底 --- */
.today-page[data-theme="inkGreen"] .inline-add-action {
  height: 88rpx;
  margin-top: 12rpx;
  border-radius: 24rpx;
  background: #F2F8F2;
  border: 1rpx solid #E4EEE5;
  color: #2F765E;
  font-size: 30rpx;
  font-weight: 800;
}

/* --- 今日数据卡 --- */
.today-page[data-theme="inkGreen"] .today-data-value {
  color: #2F765E;
  font-size: 40rpx;
  font-weight: 800;
}

/* --- 每日提示卡：浅金 --- */
.today-page[data-theme="inkGreen"] .daily-nudge-card {
  border: 1rpx solid #F5DFA5;
  background: #FFF7DC;
  border-radius: 24rpx;
}

/* --- 文字颜色统一 --- */
.today-page[data-theme="inkGreen"] .week-title,
.today-page[data-theme="inkGreen"] .task-title,
.today-page[data-theme="inkGreen"] .today-data-title,
.today-page[data-theme="inkGreen"] .state-title,
.today-page[data-theme="inkGreen"] .empty-title {
  color: #20322D;
}

.today-page[data-theme="inkGreen"] .today-subtitle,
.today-page[data-theme="inkGreen"] .action-hero-count,
.today-page[data-theme="inkGreen"] .task-desc,
.today-page[data-theme="inkGreen"] .today-data-label,
.today-page[data-theme="inkGreen"] .week-switch,
.today-page[data-theme="inkGreen"] .today-data-entry {
  color: #7D8C86;
}

/* --- 周日历今日：金色圆点 --- */
.today-page[data-theme="inkGreen"] .week-day.is-today .week-number {
  background: #F2C34B;
  color: #24352E;
  box-shadow: 0 12rpx 24rpx rgba(242, 195, 75, 0.22);
}
